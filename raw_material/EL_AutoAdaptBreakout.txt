Express EL_AutoAdaptBreakout
// Je sais, c'est codé avec les pieds...
//2018-10-13
// Etat : les signaux et mesures sont OK pour une prise de position (la première). La mesure des PV de chaque jour est OK.
// 2018-10-18 : bug empéchant une boucle de distance des objectifs (pour chaque horaire) non résolu.
// 2018-10-19 : La version 4_0 est opérationnelle pour chercher les bonnes valeurs à la main. Elle sera donnée aux participants sans limite de date.
// 2018-10-21 : on essaie de se passer de la boucle en désespoir de cause
//              La version 4_1 fonctionne correctement avec 2 possibilités de choix.
// 2018-10-23 : La version 4_2 fonctionne pour tous les choix et le seul range 8h-9h.
// 2018-10-23 : La version 4_3 fonctionne pour tous les choix et les seuls ranges 8h-9h et 9h-10h.
// 2018-10-24 : La version 4.4 fonctionne tous les choix et les 4 ranges.
// 2018-10-26 : La vesrion 5.1 est opérationnelle
// 2018-10-26 : La version 5.2 laisse le choix des horaires de range et de l'heure de sortie
// 2018-10-26 : la version 5.3 est opérationnelle complète
// 2018-10-26 : version 5.4  Une possibilité d'élimination de certains des ranges si $EndHour = 0 est rajoutée
// 2018-10-30 : ajout d'un message box à chaque nouveau jour et passage à la version définitive EL_AutoAdaptBreakout
// 2018-12-17 : fin de l'optimisation du temps machine (enfin!). reste à voir si c'est efficace...

Vars
input $BarsInADay(6,168,168); // le nombre de barres dans une journée pour l'UT étudiée
input $BarsOfRange(1,15,12); // le nombre de barres sur lequel on va examiner le haut et la bas. Si 2 bars sur 1/2heure, on examine donc la volat horaire sur un graphe 30 minutes
//input $NbTradesPerDay_max2(1,2,1); // C'est le nombre de trades que l'on veut effectuer chaque jour
input $NbDaysTested(1,14,1); // Le nombre de jours sur lesquels seront effectués le test de rentabilité du (des) réglages
input $EndHour1(00,2355,900),$EndHour2(00,2355,1000),$EndHour3(0,2355,1500),$EndHour4(0,2355,1600); // heures de fin des 4 ranges examinés
          
//      CE SERAIT BIEN QUE LE CALCUL NE SE FASSE PAS SI L'HEURE EST EGALE A ZERO : OK fait

input $HourToExit(1725,1725,1725); // heure de sortie intraday

Series haut,bas,somme,CBI;
series HautRange,BasRange,LowPrecd,HighPrecd;
Series RangeDefini; // est à 0 en début de journée, passe à 1 quand le range du jour est défini. Utilisé dans les tests de PV.

series Marqueur_IsNewDay;
series EndTime;
series test;

Numeric i,j;
series FinTemps; // heure de fin de range étudié

//Series NumSignalJour; // donne le numéro d'ordre du signal du jour : 1 pour le premier signal, 2 pour le deuxième ...
Series NumsignalJour01,NumsignalJour02,NumsignalJour03,NumsignalJour04,NumsignalJour05,NumsignalJour06,NumsignalJour07,NumsignalJour11,NumsignalJour12,NumsignalJour13,NumsignalJour14,NumsignalJour15,NumsignalJour16,NumsignalJour17;
Series NumsignalJour21,NumsignalJour22,NumsignalJour23,NumsignalJour24,NumsignalJour25,NumsignalJour26,NumsignalJour27,NumsignalJour31,NumsignalJour32,NumsignalJour33,NumsignalJour34,NumsignalJour35,NumsignalJour36,NumsignalJour37;
//Series ObjRangeTaille; // C'est la taille del'objectif du range qui sera calculé lorsque le range sera connu. Il est fonction de l'input "$ObjRange10e"
series ObjRangeTaille01,ObjRangeTaille02,ObjRangeTaille03,ObjRangeTaille04,ObjRangeTaille05,ObjRangeTaille06,ObjRangeTaille07,ObjRangeTaille11,ObjRangeTaille12,ObjRangeTaille13,ObjRangeTaille14,ObjRangeTaille15,ObjRangeTaille16,ObjRangeTaille17;
Series ObjRangeTaille21,ObjRangeTaille22,ObjRangeTaille23,ObjRangeTaille24,ObjRangeTaille25,ObjRangeTaille26,ObjRangeTaille27,ObjRangeTaille31,ObjRangeTaille32,ObjRangeTaille33,ObjRangeTaille34,ObjRangeTaille35,ObjRangeTaille36,ObjRangeTaille37;
//series ObjRange10e; // Distance en 10eme de range de l'objectif
Series ObjRange10e01,ObjRange10e02,ObjRange10e03,ObjRange10e04,ObjRange10e05,ObjRange10e06,ObjRange10e07,ObjRange10e11,ObjRange10e12,ObjRange10e13,ObjRange10e14,ObjRange10e15,ObjRange10e16,ObjRange10e17;
Series ObjRange10e21,ObjRange10e22,ObjRange10e23,ObjRange10e24,ObjRange10e25,ObjRange10e26,ObjRange10e27,ObjRange10e31,ObjRange10e32,ObjRange10e33,ObjRange10e34,ObjRange10e35,ObjRange10e36,ObjRange10e37;
//Series Pose; // est à 0 dans le range, à 100 au-dessus du range, à-100 en dessous du range
Series Pose01,Pose02,Pose03,Pose04,Pose05,Pose06,Pose07,Pose11,Pose12,Pose13,Pose14,Pose15,Pose16,Pose17;
Series Pose21,Pose22,Pose23,Pose24,Pose25,Pose26,Pose27,Pose31,Pose32,Pose33,Pose34,Pose35,Pose36,Pose37;
//Series signal; // détecte les signaux d'entrée : passage de "pose" >0 ou <0 . Prend une valeur de 0 ou 50 ou -50
Series signal01,signal02,signal03,signal04,signal05,signal06,signal07,signal11,signal12,signal13,signal14,signal15,signal16,signal17;
Series signal21,signal22,signal23,signal24,signal25,signal26,signal27,signal31,signal32,signal33,signal34,signal35,signal36,signal37;
//Series ObjHausse,ObjBaisse; //Objectifs de cours si cassure à la hausse ou à la baisse
Series ObjHausse01,ObjHausse02,ObjHausse03,ObjHausse04,ObjHausse05,ObjHausse06,ObjHausse07,ObjHausse11,ObjHausse12,ObjHausse13,ObjHausse14,ObjHausse15,ObjHausse16,ObjHausse17;
Series ObjHausse21,ObjHausse22,ObjHausse23,ObjHausse24,ObjHausse25,ObjHausse26,ObjHausse27,ObjHausse31,ObjHausse32,ObjHausse33,ObjHausse34,ObjHausse35,ObjHausse36,ObjHausse37;
Series ObjBaisse01,ObjBaisse02,ObjBaisse03,ObjBaisse04,ObjBaisse05,ObjBaisse06,ObjBaisse07,ObjBaisse11,ObjBaisse12,ObjBaisse13,ObjBaisse14,ObjBaisse15,ObjBaisse16,ObjBaisse17;
Series ObjBaisse21,ObjBaisse22,ObjBaisse23,ObjBaisse24,ObjBaisse25,ObjBaisse26,ObjBaisse27,ObjBaisse31,ObjBaisse32,ObjBaisse33,ObjBaisse34,ObjBaisse35,ObjBaisse36,ObjBaisse37;
//Series ObjHausse1,ObjBaisse1,Stop1,NiveauEntree1; // Objectif et stop première position du jour, niveau d'entrée de cette position
Series NiveauEntree1_01,NiveauEntree1_02,NiveauEntree1_03,NiveauEntree1_04,NiveauEntree1_05,NiveauEntree1_06,NiveauEntree1_07,NiveauEntree1_11,NiveauEntree1_12,NiveauEntree1_13,NiveauEntree1_14,NiveauEntree1_15,NiveauEntree1_16,NiveauEntree1_17;
Series NiveauEntree1_21,NiveauEntree1_22,NiveauEntree1_23,NiveauEntree1_24,NiveauEntree1_25,NiveauEntree1_26,NiveauEntree1_27,NiveauEntree1_31,NiveauEntree1_32,NiveauEntree1_33,NiveauEntree1_34,NiveauEntree1_35,NiveauEntree1_36,NiveauEntree1_37;
Series ObjHausse1_01,ObjHausse1_02,ObjHausse1_03,ObjHausse1_04,ObjHausse1_05,ObjHausse1_06,ObjHausse1_07,ObjHausse1_11,ObjHausse1_12,ObjHausse1_13,ObjHausse1_14,ObjHausse1_15,ObjHausse1_16,ObjHausse1_17;
Series ObjHausse1_21,ObjHausse1_22,ObjHausse1_23,ObjHausse1_24,ObjHausse1_25,ObjHausse1_26,ObjHausse1_27,ObjHausse1_31,ObjHausse1_32,ObjHausse1_33,ObjHausse1_34,ObjHausse1_35,ObjHausse1_36,ObjHausse1_37;
Series ObjBaisse1_01,ObjBaisse1_02,ObjBaisse1_03,ObjBaisse1_04,ObjBaisse1_05,ObjBaisse1_06,ObjBaisse1_07,ObjBaisse1_11,ObjBaisse1_12,ObjBaisse1_13,ObjBaisse1_14,ObjBaisse1_15,ObjBaisse1_16,ObjBaisse1_17;
Series ObjBaisse1_21,ObjBaisse1_22,ObjBaisse1_23,ObjBaisse1_24,ObjBaisse1_25,ObjBaisse1_26,ObjBaisse1_27,ObjBaisse1_31,ObjBaisse1_32,ObjBaisse1_33,ObjBaisse1_34,ObjBaisse1_35,ObjBaisse1_36,ObjBaisse1_37;
Series Stop1_01,Stop1_02,Stop1_03,Stop1_04,Stop1_05,Stop1_06,Stop1_07,Stop1_11,Stop1_12,Stop1_13,Stop1_14,Stop1_15,Stop1_16,Stop1_17;
Series Stop1_21,Stop1_22,Stop1_23,Stop1_24,Stop1_25,Stop1_26,Stop1_27,Stop1_31,Stop1_32,Stop1_33,Stop1_34,Stop1_35,Stop1_36,Stop1_37;
//Series Equity1; // +/-value de la première position du jour
Series Equity1_01,Equity1_02,Equity1_03,Equity1_04,Equity1_05,Equity1_06,Equity1_07,Equity1_11,Equity1_12,Equity1_13,Equity1_14,Equity1_15,Equity1_16,Equity1_17;
Series Equity1_21,Equity1_22,Equity1_23,Equity1_24,Equity1_25,Equity1_26,Equity1_27,Equity1_31,Equity1_32,Equity1_33,Equity1_34,Equity1_35,Equity1_36,Equity1_37;
//series Equity;  // +/-value totale du jour
Series Equity01,Equity02,Equity03,Equity04,Equity05,Equity06,Equity07,Equity11,Equity12,Equity13,Equity14,Equity15,Equity16,Equity17;
Series Equity21,Equity22,Equity23,Equity24,Equity25,Equity26,Equity27,Equity31,Equity32,Equity33,Equity34,Equity35,Equity36,Equity37;
//series EquityTot; // Total des gains sur $NbDaysTested jours
Series EquityTot01,EquityTot02,EquityTot03,EquityTot04,EquityTot05,EquityTot06,EquityTot07,EquityTot11,EquityTot12,EquityTot13,EquityTot14,EquityTot15,EquityTot16,EquityTot17;
Series EquityTot21,EquityTot22,EquityTot23,EquityTot24,EquityTot25,EquityTot26,EquityTot27,EquityTot31,EquityTot32,EquityTot33,EquityTot34,EquityTot35,EquityTot36,EquityTot37;
                                                                                              // Il faudrait avoir l'équity moyenne par jour
                                                                                              // -------------------------------------------
                                                                                              // Il faudra des séries pour l'objectif et le stop 
                                                                                              // qui seront repris dans les deux indicateurs complémentaires

series MeilleureEquity;          // meilleure equity parmi les heures et objectifs examinés
Series MeilleureHeureEtObjectif; // numéro d'ordre de la meilleure Equity
series MeilleureHeure;           // meilleure heure pour la journée en cours
series MeilleurObjectif;         // meilleur objectif pour la journée en cours
series PositionPrise;            // marqueur pour savoir si une position a été déjà prise ou pas en cours de journée. 
Series HautRangeDuJour,BasRangeDuJour; // haut et bas sélectionnés en fonction des résultats du test sur les jours précédents
Series ObjRangeTaille;           // taille de l'objectif calculé en fonction du range et du coef (MeilleurObjectif)
Series senti;                    // donnera le sentiment
Series Objectif;
Series NiveauStop;

series isnewday,BarsOfRange;        //----------------------------------------------------------------------------decembre2018  
//series timeop;

calculation

// Le programme s'exécute à la clôture de chaque barre et non à chaque tick
if IsFirstBar() then CalculateAtEveryTick(false);

CBI = currentbarindex();
LowPrecd=l[1];  
HighPrecd=h[1];
HautRangeDuJour=HautRangeDuJour[1];
BasRangeDuJour=BasRangeDuJour[1];
PositionPrise=PositionPrise[1];
ObjRangeTaille=ObjRangeTaille[1];
Objectif=Objectif[1];
NiveauStop=NiveauStop[1];
if isnewday() then isnewday=1;           //----------------------------------------------------------------------------decembre2018
barsofrange=$barsofrange;

haut=Highest(HighPrecd,barsOfRange[j]);  //----------------------------------------------------------------------------decembre2018
bas=Lowest(LowPrecd,barsOfRange[j]);     //----------------------------------------------------------------------------decembre2018
//timeop=TimeToNumeric(timeopen);


// **************************************************************************************************************************************************** Range 1

if ($EndHour1 =0) then begin
  EquityTot01=-1000;
  EquityTot02=-1000;
  EquityTot03=-1000;
  EquityTot04=-1000;    // Ici on empèche le calcul pour $EndHour2 si l'heure demandée est zéro
  EquityTot05=-1000;
  EquityTot06=-1000;
  EquityTot07=-1000;
  end
  else begin


// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre0028
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre0028

// Partie commune ====================================================================================
    FinTemps[j] = $EndHour1;
    for i=0 to ($BarsInADay) begin // calcul des plus haut et plus bas du range pour chaque jour considéré
      if (TimeToNumeric(timeopen[j]) = FinTemps[j]) then begin // Quand c'est la fin de période du range
        HautRange[j]=haut[j];
        BasRange[j]=bas[j];
      End
      else begin
        HautRange[j]=HautRange[j+1];
        BasRange[j]=BasRange[j+1];
      end
    end

// ici commence le calcul des PV pour 8

ObjRange10e01[j]=8;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille01[j] = ObjRangeTaille01[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour01[j]=NumsignalJour01[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_01[j]=NiveauEntree1_01[j+1];
ObjHausse1_01[j]=ObjHausse1_01[j+1];
ObjBaisse1_01[j]=ObjBaisse1_01[j+1];
Stop1_01[j]=Stop1_01[j+1];
Equity1_01[j]=Equity1_01[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour01[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille01[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille01[j]= (ObjRange10e01[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose01[j]=100;
 if c[j]<BasRange[j] then Pose01[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal01[j]=0;
if ((Pose01[j+1]<=0.01) and (Pose01[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille01[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal01[j]=50;
    NumsignalJour01[j]=NumsignalJour01[j]+1;                   // et on donne un numéro de signal
    ObjHausse01[j]= (HautRange[j] + ObjRangeTaille01[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose01[j+1]>=0) and (Pose01[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille01[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal01[j]=-50;
    NumsignalJour01[j]=NumsignalJour01[j]+1;                   // et on donne un numéro de signal
    ObjBaisse01[j]= (BasRange[j] - ObjRangeTaille01[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour01[j]=1) and (NumsignalJour01[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal01[j]=50) then begin // si signal d'achat
    NiveauEntree1_01[j]=c[j]; //
    ObjHausse1_01[j]=ObjHausse01[j];
    Stop1_01[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_01[j]=c[j]; //
      ObjBaisse1_01[j]=ObjBaisse01[j];
      Stop1_01[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_01[j] <>0) then begin
  if (h[j]>ObjHausse1_01[j]) then begin                // Si l'objectif a été atteint
    Equity1_01[j]=(ObjHausse1_01[j]-NiveauEntree1_01[j]);       //   on calcule la + value
    ObjHausse1_01[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_01[j]) then begin                     // Si le stop a été atteint
    Equity1_01[j]=(Stop1_01[j]-NiveauEntree1_01[j]);            //   On calcule la - value
    ObjHausse1_01[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_01[j] <>0) ) then begin 
    Equity1_01[j]=(c[j]-NiveauEntree1_01[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 01h35: On sort de position
    ObjHausse1_01[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_01[j] <>0) then begin
  if (l[j]<ObjBaisse1_01[j]) then begin              // Si l'objectif a été atteint
    Equity1_01[j]=(NiveauEntree1_01[j] - ObjBaisse1_01[j]);   //   on calcule la + value
    ObjBaisse1_01[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_01[j]) then begin                   // Si le stop a été atteint
    Equity1_01[j]=(NiveauEntree1_01[j] - Stop1_01[j]);        //   On calcule la - value
    ObjBaisse1_01[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_01[j] <>0) ) then begin 
    Equity1_01[j]=(NiveauEntree1_01[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 01h35: On sort de position
    ObjBaisse1_01[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity01[j]=Equity1_01[j];
  Equity1_01[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot01[j] = EquityTot01[j] + Equity01[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 8
end     //----------------------------------------------------------------------------decembre0028
end     //----------------------------------------------------------------------------decembre0028
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre0228
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre0228



// ici commence le calcul des PV pour 10

ObjRange10e02[j]=10;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille02[j] = ObjRangeTaille02[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour02[j]=NumsignalJour02[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_02[j]=NiveauEntree1_02[j+1];
ObjHausse1_02[j]=ObjHausse1_02[j+1];
ObjBaisse1_02[j]=ObjBaisse1_02[j+1];
Stop1_02[j]=Stop1_02[j+1];
Equity1_02[j]=Equity1_02[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour02[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille02[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille02[j]= (ObjRange10e02[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose02[j]=100;
 if c[j]<BasRange[j] then Pose02[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal02[j]=0;
if ((Pose02[j+1]<=0.02) and (Pose02[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille02[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal02[j]=50;
    NumsignalJour02[j]=NumsignalJour02[j]+1;                   // et on donne un numéro de signal
    ObjHausse02[j]= (HautRange[j] + ObjRangeTaille02[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose02[j+1]>=0) and (Pose02[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille02[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal02[j]=-50;
    NumsignalJour02[j]=NumsignalJour02[j]+1;                   // et on donne un numéro de signal
    ObjBaisse02[j]= (BasRange[j] - ObjRangeTaille02[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour02[j]=1) and (NumsignalJour02[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal02[j]=50) then begin // si signal d'achat
    NiveauEntree1_02[j]=c[j]; //
    ObjHausse1_02[j]=ObjHausse02[j];
    Stop1_02[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_02[j]=c[j]; //
      ObjBaisse1_02[j]=ObjBaisse02[j];
      Stop1_02[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_02[j] <>0) then begin
  if (h[j]>ObjHausse1_02[j]) then begin                // Si l'objectif a été atteint
    Equity1_02[j]=(ObjHausse1_02[j]-NiveauEntree1_02[j]);       //   on calcule la + value
    ObjHausse1_02[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_02[j]) then begin                     // Si le stop a été atteint
    Equity1_02[j]=(Stop1_02[j]-NiveauEntree1_02[j]);            //   On calcule la - value
    ObjHausse1_02[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_02[j] <>0) ) then begin 
    Equity1_02[j]=(c[j]-NiveauEntree1_02[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 02h35: On sort de position
    ObjHausse1_02[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_02[j] <>0) then begin
  if (l[j]<ObjBaisse1_02[j]) then begin              // Si l'objectif a été atteint
    Equity1_02[j]=(NiveauEntree1_02[j] - ObjBaisse1_02[j]);   //   on calcule la + value
    ObjBaisse1_02[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_02[j]) then begin                   // Si le stop a été atteint
    Equity1_02[j]=(NiveauEntree1_02[j] - Stop1_02[j]);        //   On calcule la - value
    ObjBaisse1_02[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_02[j] <>0) ) then begin 
    Equity1_02[j]=(NiveauEntree1_02[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 02h35: On sort de position
    ObjBaisse1_02[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity02[j]=Equity1_02[j];
  Equity1_02[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot02[j] = EquityTot02[j] + Equity02[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 10
end     //----------------------------------------------------------------------------decembre0228
end     //----------------------------------------------------------------------------decembre0228
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2018
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2018



// ici commence le calcul des PV pour 12

ObjRange10e03[j]=12;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille03[j] = ObjRangeTaille03[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour03[j]=NumsignalJour03[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_03[j]=NiveauEntree1_03[j+1];
ObjHausse1_03[j]=ObjHausse1_03[j+1];
ObjBaisse1_03[j]=ObjBaisse1_03[j+1];
Stop1_03[j]=Stop1_03[j+1];
Equity1_03[j]=Equity1_03[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour03[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille03[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille03[j]= (ObjRange10e03[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose03[j]=100;
 if c[j]<BasRange[j] then Pose03[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal03[j]=0;
if ((Pose03[j+1]<=0.03) and (Pose03[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille03[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal03[j]=50;
    NumsignalJour03[j]=NumsignalJour03[j]+1;                   // et on donne un numéro de signal
    ObjHausse03[j]= (HautRange[j] + ObjRangeTaille03[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose03[j+1]>=0) and (Pose03[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille03[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal03[j]=-50;
    NumsignalJour03[j]=NumsignalJour03[j]+1;                   // et on donne un numéro de signal
    ObjBaisse03[j]= (BasRange[j] - ObjRangeTaille03[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour03[j]=1) and (NumsignalJour03[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal03[j]=50) then begin // si signal d'achat
    NiveauEntree1_03[j]=c[j]; //
    ObjHausse1_03[j]=ObjHausse03[j];
    Stop1_03[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_03[j]=c[j]; //
      ObjBaisse1_03[j]=ObjBaisse03[j];
      Stop1_03[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_03[j] <>0) then begin
  if (h[j]>ObjHausse1_03[j]) then begin                // Si l'objectif a été atteint
    Equity1_03[j]=(ObjHausse1_03[j]-NiveauEntree1_03[j]);       //   on calcule la + value
    ObjHausse1_03[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_03[j]) then begin                     // Si le stop a été atteint
    Equity1_03[j]=(Stop1_03[j]-NiveauEntree1_03[j]);            //   On calcule la - value
    ObjHausse1_03[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_03[j] <>0) ) then begin 
    Equity1_03[j]=(c[j]-NiveauEntree1_03[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 03h35: On sort de position
    ObjHausse1_03[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_03[j] <>0) then begin
  if (l[j]<ObjBaisse1_03[j]) then begin              // Si l'objectif a été atteint
    Equity1_03[j]=(NiveauEntree1_03[j] - ObjBaisse1_03[j]);   //   on calcule la + value
    ObjBaisse1_03[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_03[j]) then begin                   // Si le stop a été atteint
    Equity1_03[j]=(NiveauEntree1_03[j] - Stop1_03[j]);        //   On calcule la - value
    ObjBaisse1_03[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_03[j] <>0) ) then begin 
    Equity1_03[j]=(NiveauEntree1_03[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 03h35: On sort de position
    ObjBaisse1_03[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity03[j]=Equity1_03[j];
  Equity1_03[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot03[j] = EquityTot03[j] + Equity03[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 12
end     //----------------------------------------------------------------------------decembre2018
end     //----------------------------------------------------------------------------decembre22018
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2018
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2018



// ici commence le calcul des PV pour 14

ObjRange10e04[j]=14;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille04[j] = ObjRangeTaille04[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour04[j]=NumsignalJour04[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_04[j]=NiveauEntree1_04[j+1];
ObjHausse1_04[j]=ObjHausse1_04[j+1];
ObjBaisse1_04[j]=ObjBaisse1_04[j+1];
Stop1_04[j]=Stop1_04[j+1];
Equity1_04[j]=Equity1_04[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour04[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille04[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille04[j]= (ObjRange10e04[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose04[j]=100;
 if c[j]<BasRange[j] then Pose04[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal04[j]=0;
if ((Pose04[j+1]<=0.04) and (Pose04[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille04[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal04[j]=50;
    NumsignalJour04[j]=NumsignalJour04[j]+1;                   // et on donne un numéro de signal
    ObjHausse04[j]= (HautRange[j] + ObjRangeTaille04[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose04[j+1]>=0) and (Pose04[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille04[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal04[j]=-50;
    NumsignalJour04[j]=NumsignalJour04[j]+1;                   // et on donne un numéro de signal
    ObjBaisse04[j]= (BasRange[j] - ObjRangeTaille04[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour04[j]=1) and (NumsignalJour04[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal04[j]=50) then begin // si signal d'achat
    NiveauEntree1_04[j]=c[j]; //
    ObjHausse1_04[j]=ObjHausse04[j];
    Stop1_04[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_04[j]=c[j]; //
      ObjBaisse1_04[j]=ObjBaisse04[j];
      Stop1_04[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_04[j] <>0) then begin
  if (h[j]>ObjHausse1_04[j]) then begin                // Si l'objectif a été atteint
    Equity1_04[j]=(ObjHausse1_04[j]-NiveauEntree1_04[j]);       //   on calcule la + value
    ObjHausse1_04[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_04[j]) then begin                     // Si le stop a été atteint
    Equity1_04[j]=(Stop1_04[j]-NiveauEntree1_04[j]);            //   On calcule la - value
    ObjHausse1_04[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_04[j] <>0) ) then begin 
    Equity1_04[j]=(c[j]-NiveauEntree1_04[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 04h35: On sort de position
    ObjHausse1_04[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_04[j] <>0) then begin
  if (l[j]<ObjBaisse1_04[j]) then begin              // Si l'objectif a été atteint
    Equity1_04[j]=(NiveauEntree1_04[j] - ObjBaisse1_04[j]);   //   on calcule la + value
    ObjBaisse1_04[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_04[j]) then begin                   // Si le stop a été atteint
    Equity1_04[j]=(NiveauEntree1_04[j] - Stop1_04[j]);        //   On calcule la - value
    ObjBaisse1_04[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_04[j] <>0) ) then begin 
    Equity1_04[j]=(NiveauEntree1_04[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 04h35: On sort de position
    ObjBaisse1_04[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity04[j]=Equity1_04[j];
  Equity1_04[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot04[j] = EquityTot04[j] + Equity04[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 14
end     //----------------------------------------------------------------------------decembre2018
end     //----------------------------------------------------------------------------decembre22018
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2018
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2018



// ici commence le calcul des PV pour 16

ObjRange10e05[j]=16;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille05[j] = ObjRangeTaille05[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour05[j]=NumsignalJour05[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_05[j]=NiveauEntree1_05[j+1];
ObjHausse1_05[j]=ObjHausse1_05[j+1];
ObjBaisse1_05[j]=ObjBaisse1_05[j+1];
Stop1_05[j]=Stop1_05[j+1];
Equity1_05[j]=Equity1_05[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour05[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille05[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille05[j]= (ObjRange10e05[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose05[j]=100;
 if c[j]<BasRange[j] then Pose05[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal05[j]=0;
if ((Pose05[j+1]<=0.05) and (Pose05[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille05[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal05[j]=50;
    NumsignalJour05[j]=NumsignalJour05[j]+1;                   // et on donne un numéro de signal
    ObjHausse05[j]= (HautRange[j] + ObjRangeTaille05[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose05[j+1]>=0) and (Pose05[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille05[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal05[j]=-50;
    NumsignalJour05[j]=NumsignalJour05[j]+1;                   // et on donne un numéro de signal
    ObjBaisse05[j]= (BasRange[j] - ObjRangeTaille05[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour05[j]=1) and (NumsignalJour05[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal05[j]=50) then begin // si signal d'achat
    NiveauEntree1_05[j]=c[j]; //
    ObjHausse1_05[j]=ObjHausse05[j];
    Stop1_05[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_05[j]=c[j]; //
      ObjBaisse1_05[j]=ObjBaisse05[j];
      Stop1_05[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_05[j] <>0) then begin
  if (h[j]>ObjHausse1_05[j]) then begin                // Si l'objectif a été atteint
    Equity1_05[j]=(ObjHausse1_05[j]-NiveauEntree1_05[j]);       //   on calcule la + value
    ObjHausse1_05[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_05[j]) then begin                     // Si le stop a été atteint
    Equity1_05[j]=(Stop1_05[j]-NiveauEntree1_05[j]);            //   On calcule la - value
    ObjHausse1_05[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_05[j] <>0) ) then begin 
    Equity1_05[j]=(c[j]-NiveauEntree1_05[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 05h35: On sort de position
    ObjHausse1_05[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_05[j] <>0) then begin
  if (l[j]<ObjBaisse1_05[j]) then begin              // Si l'objectif a été atteint
    Equity1_05[j]=(NiveauEntree1_05[j] - ObjBaisse1_05[j]);   //   on calcule la + value
    ObjBaisse1_05[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_05[j]) then begin                   // Si le stop a été atteint
    Equity1_05[j]=(NiveauEntree1_05[j] - Stop1_05[j]);        //   On calcule la - value
    ObjBaisse1_05[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_05[j] <>0) ) then begin 
    Equity1_05[j]=(NiveauEntree1_05[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 05h35: On sort de position
    ObjBaisse1_05[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity05[j]=Equity1_05[j];
  Equity1_05[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot05[j] = EquityTot05[j] + Equity05[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 16
end     //----------------------------------------------------------------------------decembre2018
end     //----------------------------------------------------------------------------decembre22018
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2018
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2018



// ici commence le calcul des PV pour 18

ObjRange10e06[j]=18;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille06[j] = ObjRangeTaille06[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour06[j]=NumsignalJour06[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_06[j]=NiveauEntree1_06[j+1];
ObjHausse1_06[j]=ObjHausse1_06[j+1];
ObjBaisse1_06[j]=ObjBaisse1_06[j+1];
Stop1_06[j]=Stop1_06[j+1];
Equity1_06[j]=Equity1_06[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour06[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille06[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille06[j]= (ObjRange10e06[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose06[j]=100;
 if c[j]<BasRange[j] then Pose06[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal06[j]=0;
if ((Pose06[j+1]<=0.06) and (Pose06[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille06[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal06[j]=50;
    NumsignalJour06[j]=NumsignalJour06[j]+1;                   // et on donne un numéro de signal
    ObjHausse06[j]= (HautRange[j] + ObjRangeTaille06[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose06[j+1]>=0) and (Pose06[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille06[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal06[j]=-50;
    NumsignalJour06[j]=NumsignalJour06[j]+1;                   // et on donne un numéro de signal
    ObjBaisse06[j]= (BasRange[j] - ObjRangeTaille06[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour06[j]=1) and (NumsignalJour06[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal06[j]=50) then begin // si signal d'achat
    NiveauEntree1_06[j]=c[j]; //
    ObjHausse1_06[j]=ObjHausse06[j];
    Stop1_06[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_06[j]=c[j]; //
      ObjBaisse1_06[j]=ObjBaisse06[j];
      Stop1_06[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_06[j] <>0) then begin
  if (h[j]>ObjHausse1_06[j]) then begin                // Si l'objectif a été atteint
    Equity1_06[j]=(ObjHausse1_06[j]-NiveauEntree1_06[j]);       //   on calcule la + value
    ObjHausse1_06[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_06[j]) then begin                     // Si le stop a été atteint
    Equity1_06[j]=(Stop1_06[j]-NiveauEntree1_06[j]);            //   On calcule la - value
    ObjHausse1_06[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_06[j] <>0) ) then begin 
    Equity1_06[j]=(c[j]-NiveauEntree1_06[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 06h35: On sort de position
    ObjHausse1_06[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_06[j] <>0) then begin
  if (l[j]<ObjBaisse1_06[j]) then begin              // Si l'objectif a été atteint
    Equity1_06[j]=(NiveauEntree1_06[j] - ObjBaisse1_06[j]);   //   on calcule la + value
    ObjBaisse1_06[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_06[j]) then begin                   // Si le stop a été atteint
    Equity1_06[j]=(NiveauEntree1_06[j] - Stop1_06[j]);        //   On calcule la - value
    ObjBaisse1_06[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_06[j] <>0) ) then begin 
    Equity1_06[j]=(NiveauEntree1_06[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 06h35: On sort de position
    ObjBaisse1_06[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity06[j]=Equity1_06[j];
  Equity1_06[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot06[j] = EquityTot06[j] + Equity06[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 18
end     //----------------------------------------------------------------------------decembre2018
end     //----------------------------------------------------------------------------decembre2018
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2018
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2018


ObjRange10e07[j]=20;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille07[j] = ObjRangeTaille07[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour07[j]=NumsignalJour07[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_07[j]=NiveauEntree1_07[j+1];
ObjHausse1_07[j]=ObjHausse1_07[j+1];
ObjBaisse1_07[j]=ObjBaisse1_07[j+1];
Stop1_07[j]=Stop1_07[j+1];
Equity1_07[j]=Equity1_07[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour07[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille07[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille07[j]= (ObjRange10e07[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose07[j]=100;
 if c[j]<BasRange[j] then Pose07[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal07[j]=0;
if ((Pose07[j+1]<=0.07) and (Pose07[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille07[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal07[j]=50;
    NumsignalJour07[j]=NumsignalJour07[j]+1;                   // et on donne un numéro de signal
    ObjHausse07[j]= (HautRange[j] + ObjRangeTaille07[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose07[j+1]>=0) and (Pose07[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille07[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal07[j]=-50;
    NumsignalJour07[j]=NumsignalJour07[j]+1;                   // et on donne un numéro de signal
    ObjBaisse07[j]= (BasRange[j] - ObjRangeTaille07[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour07[j]=1) and (NumsignalJour07[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal07[j]=50) then begin // si signal d'achat
    NiveauEntree1_07[j]=c[j]; //
    ObjHausse1_07[j]=ObjHausse07[j];
    Stop1_07[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_07[j]=c[j]; //
      ObjBaisse1_07[j]=ObjBaisse07[j];
      Stop1_07[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_07[j] <>0) then begin
  if (h[j]>ObjHausse1_07[j]) then begin                // Si l'objectif a été atteint
    Equity1_07[j]=(ObjHausse1_07[j]-NiveauEntree1_07[j]);       //   on calcule la + value
    ObjHausse1_07[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_07[j]) then begin                     // Si le stop a été atteint
    Equity1_07[j]=(Stop1_07[j]-NiveauEntree1_07[j]);            //   On calcule la - value
    ObjHausse1_07[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_07[j] <>0) ) then begin 
    Equity1_07[j]=(c[j]-NiveauEntree1_07[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 07h35: On sort de position
    ObjHausse1_07[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_07[j] <>0) then begin
  if (l[j]<ObjBaisse1_07[j]) then begin              // Si l'objectif a été atteint
    Equity1_07[j]=(NiveauEntree1_07[j] - ObjBaisse1_07[j]);   //   on calcule la + value
    ObjBaisse1_07[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_07[j]) then begin                   // Si le stop a été atteint
    Equity1_07[j]=(NiveauEntree1_07[j] - Stop1_07[j]);        //   On calcule la - value
    ObjBaisse1_07[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_07[j] <>0) ) then begin 
    Equity1_07[j]=(NiveauEntree1_07[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 07h35: On sort de position
    ObjBaisse1_07[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity07[j]=Equity1_07[j];
  Equity1_07[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot07[j] = EquityTot07[j] + Equity07[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 20
end     //----------------------------------------------------------------------------decembre2018
end     //----------------------------------------------------------------------------decembre2018
// ===================================================================================================

// ===================================================================================================


// ===================================================================================================

  end  // C'est le end de la condition qui shunte le calcul si l'heure demandée pour le range est zéro

// ************************************************************************************************************************************************* Fin Range 1


// **************************************************************************************************************************************************** Range 2

if ($EndHour2 =0) then begin
  EquityTot11=-1000;
  EquityTot12=-1000;
  EquityTot13=-1000;
  EquityTot14=-1000;    // Ici on empèche le calcul pour $EndHour2 si l'heure demandée est zéro
  EquityTot15=-1000;
  EquityTot16=-1000;
  EquityTot17=-1000;
  end
  else begin


// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre0128
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre0128

// Partie commune ====================================================================================
    FinTemps[j] = $EndHour2;
    for i=0 to ($BarsInADay) begin // calcul des plus haut et plus bas du range pour chaque jour considéré
      if (TimeToNumeric(timeopen[j]) = FinTemps[j]) then begin // Quand c'est la fin de période du range
        HautRange[j]=haut[j];
        BasRange[j]=bas[j];
      End
      else begin
        HautRange[j]=HautRange[j+1];
        BasRange[j]=BasRange[j+1];
      end
    end

// ici commence le calcul des PV pour 8

ObjRange10e11[j]=8;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille11[j] = ObjRangeTaille11[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour11[j]=NumsignalJour11[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_11[j]=NiveauEntree1_11[j+1];
ObjHausse1_11[j]=ObjHausse1_11[j+1];
ObjBaisse1_11[j]=ObjBaisse1_11[j+1];
Stop1_11[j]=Stop1_11[j+1];
Equity1_11[j]=Equity1_11[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour11[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille11[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille11[j]= (ObjRange10e11[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose11[j]=100;
 if c[j]<BasRange[j] then Pose11[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal11[j]=0;
if ((Pose11[j+1]<=0.11) and (Pose11[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille11[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal11[j]=50;
    NumsignalJour11[j]=NumsignalJour11[j]+1;                   // et on donne un numéro de signal
    ObjHausse11[j]= (HautRange[j] + ObjRangeTaille11[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose11[j+1]>=0) and (Pose11[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille11[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal11[j]=-50;
    NumsignalJour11[j]=NumsignalJour11[j]+1;                   // et on donne un numéro de signal
    ObjBaisse11[j]= (BasRange[j] - ObjRangeTaille11[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour11[j]=1) and (NumsignalJour11[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal11[j]=50) then begin // si signal d'achat
    NiveauEntree1_11[j]=c[j]; //
    ObjHausse1_11[j]=ObjHausse11[j];
    Stop1_11[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_11[j]=c[j]; //
      ObjBaisse1_11[j]=ObjBaisse11[j];
      Stop1_11[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_11[j] <>0) then begin
  if (h[j]>ObjHausse1_11[j]) then begin                // Si l'objectif a été atteint
    Equity1_11[j]=(ObjHausse1_11[j]-NiveauEntree1_11[j]);       //   on calcule la + value
    ObjHausse1_11[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_11[j]) then begin                     // Si le stop a été atteint
    Equity1_11[j]=(Stop1_11[j]-NiveauEntree1_11[j]);            //   On calcule la - value
    ObjHausse1_11[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_11[j] <>0) ) then begin 
    Equity1_11[j]=(c[j]-NiveauEntree1_11[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 11h35: On sort de position
    ObjHausse1_11[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_11[j] <>0) then begin
  if (l[j]<ObjBaisse1_11[j]) then begin              // Si l'objectif a été atteint
    Equity1_11[j]=(NiveauEntree1_11[j] - ObjBaisse1_11[j]);   //   on calcule la + value
    ObjBaisse1_11[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_11[j]) then begin                   // Si le stop a été atteint
    Equity1_11[j]=(NiveauEntree1_11[j] - Stop1_11[j]);        //   On calcule la - value
    ObjBaisse1_11[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_11[j] <>0) ) then begin 
    Equity1_11[j]=(NiveauEntree1_11[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 11h35: On sort de position
    ObjBaisse1_11[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity11[j]=Equity1_11[j];
  Equity1_11[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot11[j] = EquityTot11[j] + Equity11[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 8
end     //----------------------------------------------------------------------------decembre0128
end     //----------------------------------------------------------------------------decembre0128
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre1228
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre1228



// ici commence le calcul des PV pour 10

ObjRange10e12[j]=10;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille12[j] = ObjRangeTaille12[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour12[j]=NumsignalJour12[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_12[j]=NiveauEntree1_12[j+1];
ObjHausse1_12[j]=ObjHausse1_12[j+1];
ObjBaisse1_12[j]=ObjBaisse1_12[j+1];
Stop1_12[j]=Stop1_12[j+1];
Equity1_12[j]=Equity1_12[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour12[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille12[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille12[j]= (ObjRange10e12[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose12[j]=100;
 if c[j]<BasRange[j] then Pose12[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal12[j]=0;
if ((Pose12[j+1]<=0.12) and (Pose12[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille12[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal12[j]=50;
    NumsignalJour12[j]=NumsignalJour12[j]+1;                   // et on donne un numéro de signal
    ObjHausse12[j]= (HautRange[j] + ObjRangeTaille12[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose12[j+1]>=0) and (Pose12[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille12[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal12[j]=-50;
    NumsignalJour12[j]=NumsignalJour12[j]+1;                   // et on donne un numéro de signal
    ObjBaisse12[j]= (BasRange[j] - ObjRangeTaille12[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour12[j]=1) and (NumsignalJour12[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal12[j]=50) then begin // si signal d'achat
    NiveauEntree1_12[j]=c[j]; //
    ObjHausse1_12[j]=ObjHausse12[j];
    Stop1_12[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_12[j]=c[j]; //
      ObjBaisse1_12[j]=ObjBaisse12[j];
      Stop1_12[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_12[j] <>0) then begin
  if (h[j]>ObjHausse1_12[j]) then begin                // Si l'objectif a été atteint
    Equity1_12[j]=(ObjHausse1_12[j]-NiveauEntree1_12[j]);       //   on calcule la + value
    ObjHausse1_12[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_12[j]) then begin                     // Si le stop a été atteint
    Equity1_12[j]=(Stop1_12[j]-NiveauEntree1_12[j]);            //   On calcule la - value
    ObjHausse1_12[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_12[j] <>0) ) then begin 
    Equity1_12[j]=(c[j]-NiveauEntree1_12[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 12h35: On sort de position
    ObjHausse1_12[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_12[j] <>0) then begin
  if (l[j]<ObjBaisse1_12[j]) then begin              // Si l'objectif a été atteint
    Equity1_12[j]=(NiveauEntree1_12[j] - ObjBaisse1_12[j]);   //   on calcule la + value
    ObjBaisse1_12[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_12[j]) then begin                   // Si le stop a été atteint
    Equity1_12[j]=(NiveauEntree1_12[j] - Stop1_12[j]);        //   On calcule la - value
    ObjBaisse1_12[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_12[j] <>0) ) then begin 
    Equity1_12[j]=(NiveauEntree1_12[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 12h35: On sort de position
    ObjBaisse1_12[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity12[j]=Equity1_12[j];
  Equity1_12[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot12[j] = EquityTot12[j] + Equity12[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 10
end     //----------------------------------------------------------------------------decembre1228
end     //----------------------------------------------------------------------------decembre1228
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2118
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2118



// ici commence le calcul des PV pour 12

ObjRange10e13[j]=12;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille13[j] = ObjRangeTaille13[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour13[j]=NumsignalJour13[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_13[j]=NiveauEntree1_13[j+1];
ObjHausse1_13[j]=ObjHausse1_13[j+1];
ObjBaisse1_13[j]=ObjBaisse1_13[j+1];
Stop1_13[j]=Stop1_13[j+1];
Equity1_13[j]=Equity1_13[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour13[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille13[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille13[j]= (ObjRange10e13[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose13[j]=100;
 if c[j]<BasRange[j] then Pose13[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal13[j]=0;
if ((Pose13[j+1]<=0.13) and (Pose13[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille13[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal13[j]=50;
    NumsignalJour13[j]=NumsignalJour13[j]+1;                   // et on donne un numéro de signal
    ObjHausse13[j]= (HautRange[j] + ObjRangeTaille13[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose13[j+1]>=0) and (Pose13[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille13[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal13[j]=-50;
    NumsignalJour13[j]=NumsignalJour13[j]+1;                   // et on donne un numéro de signal
    ObjBaisse13[j]= (BasRange[j] - ObjRangeTaille13[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour13[j]=1) and (NumsignalJour13[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal13[j]=50) then begin // si signal d'achat
    NiveauEntree1_13[j]=c[j]; //
    ObjHausse1_13[j]=ObjHausse13[j];
    Stop1_13[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_13[j]=c[j]; //
      ObjBaisse1_13[j]=ObjBaisse13[j];
      Stop1_13[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_13[j] <>0) then begin
  if (h[j]>ObjHausse1_13[j]) then begin                // Si l'objectif a été atteint
    Equity1_13[j]=(ObjHausse1_13[j]-NiveauEntree1_13[j]);       //   on calcule la + value
    ObjHausse1_13[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_13[j]) then begin                     // Si le stop a été atteint
    Equity1_13[j]=(Stop1_13[j]-NiveauEntree1_13[j]);            //   On calcule la - value
    ObjHausse1_13[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_13[j] <>0) ) then begin 
    Equity1_13[j]=(c[j]-NiveauEntree1_13[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 13h35: On sort de position
    ObjHausse1_13[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_13[j] <>0) then begin
  if (l[j]<ObjBaisse1_13[j]) then begin              // Si l'objectif a été atteint
    Equity1_13[j]=(NiveauEntree1_13[j] - ObjBaisse1_13[j]);   //   on calcule la + value
    ObjBaisse1_13[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_13[j]) then begin                   // Si le stop a été atteint
    Equity1_13[j]=(NiveauEntree1_13[j] - Stop1_13[j]);        //   On calcule la - value
    ObjBaisse1_13[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_13[j] <>0) ) then begin 
    Equity1_13[j]=(NiveauEntree1_13[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 13h35: On sort de position
    ObjBaisse1_13[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity13[j]=Equity1_13[j];
  Equity1_13[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot13[j] = EquityTot13[j] + Equity13[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 12
end     //----------------------------------------------------------------------------decembre2118
end     //----------------------------------------------------------------------------decembre22118
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2118
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2118



// ici commence le calcul des PV pour 14

ObjRange10e14[j]=14;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille14[j] = ObjRangeTaille14[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour14[j]=NumsignalJour14[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_14[j]=NiveauEntree1_14[j+1];
ObjHausse1_14[j]=ObjHausse1_14[j+1];
ObjBaisse1_14[j]=ObjBaisse1_14[j+1];
Stop1_14[j]=Stop1_14[j+1];
Equity1_14[j]=Equity1_14[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour14[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille14[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille14[j]= (ObjRange10e14[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose14[j]=100;
 if c[j]<BasRange[j] then Pose14[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal14[j]=0;
if ((Pose14[j+1]<=0.14) and (Pose14[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille14[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal14[j]=50;
    NumsignalJour14[j]=NumsignalJour14[j]+1;                   // et on donne un numéro de signal
    ObjHausse14[j]= (HautRange[j] + ObjRangeTaille14[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose14[j+1]>=0) and (Pose14[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille14[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal14[j]=-50;
    NumsignalJour14[j]=NumsignalJour14[j]+1;                   // et on donne un numéro de signal
    ObjBaisse14[j]= (BasRange[j] - ObjRangeTaille14[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour14[j]=1) and (NumsignalJour14[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal14[j]=50) then begin // si signal d'achat
    NiveauEntree1_14[j]=c[j]; //
    ObjHausse1_14[j]=ObjHausse14[j];
    Stop1_14[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_14[j]=c[j]; //
      ObjBaisse1_14[j]=ObjBaisse14[j];
      Stop1_14[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_14[j] <>0) then begin
  if (h[j]>ObjHausse1_14[j]) then begin                // Si l'objectif a été atteint
    Equity1_14[j]=(ObjHausse1_14[j]-NiveauEntree1_14[j]);       //   on calcule la + value
    ObjHausse1_14[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_14[j]) then begin                     // Si le stop a été atteint
    Equity1_14[j]=(Stop1_14[j]-NiveauEntree1_14[j]);            //   On calcule la - value
    ObjHausse1_14[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_14[j] <>0) ) then begin 
    Equity1_14[j]=(c[j]-NiveauEntree1_14[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 14h35: On sort de position
    ObjHausse1_14[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_14[j] <>0) then begin
  if (l[j]<ObjBaisse1_14[j]) then begin              // Si l'objectif a été atteint
    Equity1_14[j]=(NiveauEntree1_14[j] - ObjBaisse1_14[j]);   //   on calcule la + value
    ObjBaisse1_14[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_14[j]) then begin                   // Si le stop a été atteint
    Equity1_14[j]=(NiveauEntree1_14[j] - Stop1_14[j]);        //   On calcule la - value
    ObjBaisse1_14[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_14[j] <>0) ) then begin 
    Equity1_14[j]=(NiveauEntree1_14[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 14h35: On sort de position
    ObjBaisse1_14[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity14[j]=Equity1_14[j];
  Equity1_14[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot14[j] = EquityTot14[j] + Equity14[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 14
end     //----------------------------------------------------------------------------decembre2118
end     //----------------------------------------------------------------------------decembre22118
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2118
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2118



// ici commence le calcul des PV pour 16

ObjRange10e15[j]=16;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille15[j] = ObjRangeTaille15[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour15[j]=NumsignalJour15[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_15[j]=NiveauEntree1_15[j+1];
ObjHausse1_15[j]=ObjHausse1_15[j+1];
ObjBaisse1_15[j]=ObjBaisse1_15[j+1];
Stop1_15[j]=Stop1_15[j+1];
Equity1_15[j]=Equity1_15[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour15[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille15[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille15[j]= (ObjRange10e15[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose15[j]=100;
 if c[j]<BasRange[j] then Pose15[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal15[j]=0;
if ((Pose15[j+1]<=0.15) and (Pose15[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille15[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal15[j]=50;
    NumsignalJour15[j]=NumsignalJour15[j]+1;                   // et on donne un numéro de signal
    ObjHausse15[j]= (HautRange[j] + ObjRangeTaille15[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose15[j+1]>=0) and (Pose15[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille15[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal15[j]=-50;
    NumsignalJour15[j]=NumsignalJour15[j]+1;                   // et on donne un numéro de signal
    ObjBaisse15[j]= (BasRange[j] - ObjRangeTaille15[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour15[j]=1) and (NumsignalJour15[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal15[j]=50) then begin // si signal d'achat
    NiveauEntree1_15[j]=c[j]; //
    ObjHausse1_15[j]=ObjHausse15[j];
    Stop1_15[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_15[j]=c[j]; //
      ObjBaisse1_15[j]=ObjBaisse15[j];
      Stop1_15[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_15[j] <>0) then begin
  if (h[j]>ObjHausse1_15[j]) then begin                // Si l'objectif a été atteint
    Equity1_15[j]=(ObjHausse1_15[j]-NiveauEntree1_15[j]);       //   on calcule la + value
    ObjHausse1_15[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_15[j]) then begin                     // Si le stop a été atteint
    Equity1_15[j]=(Stop1_15[j]-NiveauEntree1_15[j]);            //   On calcule la - value
    ObjHausse1_15[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_15[j] <>0) ) then begin 
    Equity1_15[j]=(c[j]-NiveauEntree1_15[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 15h35: On sort de position
    ObjHausse1_15[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_15[j] <>0) then begin
  if (l[j]<ObjBaisse1_15[j]) then begin              // Si l'objectif a été atteint
    Equity1_15[j]=(NiveauEntree1_15[j] - ObjBaisse1_15[j]);   //   on calcule la + value
    ObjBaisse1_15[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_15[j]) then begin                   // Si le stop a été atteint
    Equity1_15[j]=(NiveauEntree1_15[j] - Stop1_15[j]);        //   On calcule la - value
    ObjBaisse1_15[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_15[j] <>0) ) then begin 
    Equity1_15[j]=(NiveauEntree1_15[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 15h35: On sort de position
    ObjBaisse1_15[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity15[j]=Equity1_15[j];
  Equity1_15[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot15[j] = EquityTot15[j] + Equity15[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 16
end     //----------------------------------------------------------------------------decembre2118
end     //----------------------------------------------------------------------------decembre22118
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2118
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2118



// ici commence le calcul des PV pour 18

ObjRange10e16[j]=18;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille16[j] = ObjRangeTaille16[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour16[j]=NumsignalJour16[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_16[j]=NiveauEntree1_16[j+1];
ObjHausse1_16[j]=ObjHausse1_16[j+1];
ObjBaisse1_16[j]=ObjBaisse1_16[j+1];
Stop1_16[j]=Stop1_16[j+1];
Equity1_16[j]=Equity1_16[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour16[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille16[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille16[j]= (ObjRange10e16[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose16[j]=100;
 if c[j]<BasRange[j] then Pose16[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal16[j]=0;
if ((Pose16[j+1]<=0.16) and (Pose16[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille16[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal16[j]=50;
    NumsignalJour16[j]=NumsignalJour16[j]+1;                   // et on donne un numéro de signal
    ObjHausse16[j]= (HautRange[j] + ObjRangeTaille16[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose16[j+1]>=0) and (Pose16[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille16[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal16[j]=-50;
    NumsignalJour16[j]=NumsignalJour16[j]+1;                   // et on donne un numéro de signal
    ObjBaisse16[j]= (BasRange[j] - ObjRangeTaille16[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour16[j]=1) and (NumsignalJour16[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal16[j]=50) then begin // si signal d'achat
    NiveauEntree1_16[j]=c[j]; //
    ObjHausse1_16[j]=ObjHausse16[j];
    Stop1_16[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_16[j]=c[j]; //
      ObjBaisse1_16[j]=ObjBaisse16[j];
      Stop1_16[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_16[j] <>0) then begin
  if (h[j]>ObjHausse1_16[j]) then begin                // Si l'objectif a été atteint
    Equity1_16[j]=(ObjHausse1_16[j]-NiveauEntree1_16[j]);       //   on calcule la + value
    ObjHausse1_16[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_16[j]) then begin                     // Si le stop a été atteint
    Equity1_16[j]=(Stop1_16[j]-NiveauEntree1_16[j]);            //   On calcule la - value
    ObjHausse1_16[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_16[j] <>0) ) then begin 
    Equity1_16[j]=(c[j]-NiveauEntree1_16[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 16h35: On sort de position
    ObjHausse1_16[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_16[j] <>0) then begin
  if (l[j]<ObjBaisse1_16[j]) then begin              // Si l'objectif a été atteint
    Equity1_16[j]=(NiveauEntree1_16[j] - ObjBaisse1_16[j]);   //   on calcule la + value
    ObjBaisse1_16[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_16[j]) then begin                   // Si le stop a été atteint
    Equity1_16[j]=(NiveauEntree1_16[j] - Stop1_16[j]);        //   On calcule la - value
    ObjBaisse1_16[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_16[j] <>0) ) then begin 
    Equity1_16[j]=(NiveauEntree1_16[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 16h35: On sort de position
    ObjBaisse1_16[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity16[j]=Equity1_16[j];
  Equity1_16[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot16[j] = EquityTot16[j] + Equity16[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 18
end     //----------------------------------------------------------------------------decembre2118
end     //----------------------------------------------------------------------------decembre2118
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2118
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2118


ObjRange10e17[j]=20;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille17[j] = ObjRangeTaille17[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour17[j]=NumsignalJour17[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_17[j]=NiveauEntree1_17[j+1];
ObjHausse1_17[j]=ObjHausse1_17[j+1];
ObjBaisse1_17[j]=ObjBaisse1_17[j+1];
Stop1_17[j]=Stop1_17[j+1];
Equity1_17[j]=Equity1_17[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour17[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille17[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille17[j]= (ObjRange10e17[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose17[j]=100;
 if c[j]<BasRange[j] then Pose17[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal17[j]=0;
if ((Pose17[j+1]<=0.17) and (Pose17[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille17[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal17[j]=50;
    NumsignalJour17[j]=NumsignalJour17[j]+1;                   // et on donne un numéro de signal
    ObjHausse17[j]= (HautRange[j] + ObjRangeTaille17[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose17[j+1]>=0) and (Pose17[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille17[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal17[j]=-50;
    NumsignalJour17[j]=NumsignalJour17[j]+1;                   // et on donne un numéro de signal
    ObjBaisse17[j]= (BasRange[j] - ObjRangeTaille17[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour17[j]=1) and (NumsignalJour17[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal17[j]=50) then begin // si signal d'achat
    NiveauEntree1_17[j]=c[j]; //
    ObjHausse1_17[j]=ObjHausse17[j];
    Stop1_17[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_17[j]=c[j]; //
      ObjBaisse1_17[j]=ObjBaisse17[j];
      Stop1_17[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_17[j] <>0) then begin
  if (h[j]>ObjHausse1_17[j]) then begin                // Si l'objectif a été atteint
    Equity1_17[j]=(ObjHausse1_17[j]-NiveauEntree1_17[j]);       //   on calcule la + value
    ObjHausse1_17[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_17[j]) then begin                     // Si le stop a été atteint
    Equity1_17[j]=(Stop1_17[j]-NiveauEntree1_17[j]);            //   On calcule la - value
    ObjHausse1_17[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_17[j] <>0) ) then begin 
    Equity1_17[j]=(c[j]-NiveauEntree1_17[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 17h35: On sort de position
    ObjHausse1_17[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_17[j] <>0) then begin
  if (l[j]<ObjBaisse1_17[j]) then begin              // Si l'objectif a été atteint
    Equity1_17[j]=(NiveauEntree1_17[j] - ObjBaisse1_17[j]);   //   on calcule la + value
    ObjBaisse1_17[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_17[j]) then begin                   // Si le stop a été atteint
    Equity1_17[j]=(NiveauEntree1_17[j] - Stop1_17[j]);        //   On calcule la - value
    ObjBaisse1_17[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_17[j] <>0) ) then begin 
    Equity1_17[j]=(NiveauEntree1_17[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 17h35: On sort de position
    ObjBaisse1_17[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity17[j]=Equity1_17[j];
  Equity1_17[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot17[j] = EquityTot17[j] + Equity17[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 20
end     //----------------------------------------------------------------------------decembre2118
end     //----------------------------------------------------------------------------decembre2118
// ===================================================================================================

// ===================================================================================================


// ===================================================================================================

  end  // C'est le end de la condition qui shunte le calcul si l'heure demandée pour le range est zéro

// ************************************************************************************************************************************************* Fin Range 2


// **************************************************************************************************************************************************** Range 3

if ($EndHour3 =0) then begin
  EquityTot21=-1000;
  EquityTot22=-1000;
  EquityTot23=-1000;
  EquityTot24=-1000;    // Ici on empèche le calcul pour $EndHour2 si l'heure demandée est zéro
  EquityTot25=-1000;
  EquityTot26=-1000;
  EquityTot27=-1000;
  end
  else begin


// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre0228
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre0228

// Partie commune ====================================================================================
    FinTemps[j] = $EndHour3;
    for i=0 to ($BarsInADay) begin // calcul des plus haut et plus bas du range pour chaque jour considéré
      if (TimeToNumeric(timeopen[j]) = FinTemps[j]) then begin // Quand c'est la fin de période du range
        HautRange[j]=haut[j];
        BasRange[j]=bas[j];
      End
      else begin
        HautRange[j]=HautRange[j+1];
        BasRange[j]=BasRange[j+1];
      end
    end

// ici commence le calcul des PV pour 8

ObjRange10e21[j]=8;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille21[j] = ObjRangeTaille21[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour21[j]=NumsignalJour21[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_21[j]=NiveauEntree1_21[j+1];
ObjHausse1_21[j]=ObjHausse1_21[j+1];
ObjBaisse1_21[j]=ObjBaisse1_21[j+1];
Stop1_21[j]=Stop1_21[j+1];
Equity1_21[j]=Equity1_21[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour21[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille21[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille21[j]= (ObjRange10e21[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose21[j]=100;
 if c[j]<BasRange[j] then Pose21[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal21[j]=0;
if ((Pose21[j+1]<=0.21) and (Pose21[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille21[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal21[j]=50;
    NumsignalJour21[j]=NumsignalJour21[j]+1;                   // et on donne un numéro de signal
    ObjHausse21[j]= (HautRange[j] + ObjRangeTaille21[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose21[j+1]>=0) and (Pose21[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille21[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal21[j]=-50;
    NumsignalJour21[j]=NumsignalJour21[j]+1;                   // et on donne un numéro de signal
    ObjBaisse21[j]= (BasRange[j] - ObjRangeTaille21[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour21[j]=1) and (NumsignalJour21[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal21[j]=50) then begin // si signal d'achat
    NiveauEntree1_21[j]=c[j]; //
    ObjHausse1_21[j]=ObjHausse21[j];
    Stop1_21[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_21[j]=c[j]; //
      ObjBaisse1_21[j]=ObjBaisse21[j];
      Stop1_21[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_21[j] <>0) then begin
  if (h[j]>ObjHausse1_21[j]) then begin                // Si l'objectif a été atteint
    Equity1_21[j]=(ObjHausse1_21[j]-NiveauEntree1_21[j]);       //   on calcule la + value
    ObjHausse1_21[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_21[j]) then begin                     // Si le stop a été atteint
    Equity1_21[j]=(Stop1_21[j]-NiveauEntree1_21[j]);            //   On calcule la - value
    ObjHausse1_21[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_21[j] <>0) ) then begin 
    Equity1_21[j]=(c[j]-NiveauEntree1_21[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 21h35: On sort de position
    ObjHausse1_21[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_21[j] <>0) then begin
  if (l[j]<ObjBaisse1_21[j]) then begin              // Si l'objectif a été atteint
    Equity1_21[j]=(NiveauEntree1_21[j] - ObjBaisse1_21[j]);   //   on calcule la + value
    ObjBaisse1_21[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_21[j]) then begin                   // Si le stop a été atteint
    Equity1_21[j]=(NiveauEntree1_21[j] - Stop1_21[j]);        //   On calcule la - value
    ObjBaisse1_21[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_21[j] <>0) ) then begin 
    Equity1_21[j]=(NiveauEntree1_21[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 21h35: On sort de position
    ObjBaisse1_21[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity21[j]=Equity1_21[j];
  Equity1_21[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot21[j] = EquityTot21[j] + Equity21[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 8
end     //----------------------------------------------------------------------------decembre0228
end     //----------------------------------------------------------------------------decembre0228
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2228
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2228



// ici commence le calcul des PV pour 10

ObjRange10e22[j]=10;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille22[j] = ObjRangeTaille22[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour22[j]=NumsignalJour22[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_22[j]=NiveauEntree1_22[j+1];
ObjHausse1_22[j]=ObjHausse1_22[j+1];
ObjBaisse1_22[j]=ObjBaisse1_22[j+1];
Stop1_22[j]=Stop1_22[j+1];
Equity1_22[j]=Equity1_22[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour22[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille22[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille22[j]= (ObjRange10e22[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose22[j]=100;
 if c[j]<BasRange[j] then Pose22[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal22[j]=0;
if ((Pose22[j+1]<=0.22) and (Pose22[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille22[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal22[j]=50;
    NumsignalJour22[j]=NumsignalJour22[j]+1;                   // et on donne un numéro de signal
    ObjHausse22[j]= (HautRange[j] + ObjRangeTaille22[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose22[j+1]>=0) and (Pose22[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille22[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal22[j]=-50;
    NumsignalJour22[j]=NumsignalJour22[j]+1;                   // et on donne un numéro de signal
    ObjBaisse22[j]= (BasRange[j] - ObjRangeTaille22[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour22[j]=1) and (NumsignalJour22[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal22[j]=50) then begin // si signal d'achat
    NiveauEntree1_22[j]=c[j]; //
    ObjHausse1_22[j]=ObjHausse22[j];
    Stop1_22[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_22[j]=c[j]; //
      ObjBaisse1_22[j]=ObjBaisse22[j];
      Stop1_22[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_22[j] <>0) then begin
  if (h[j]>ObjHausse1_22[j]) then begin                // Si l'objectif a été atteint
    Equity1_22[j]=(ObjHausse1_22[j]-NiveauEntree1_22[j]);       //   on calcule la + value
    ObjHausse1_22[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_22[j]) then begin                     // Si le stop a été atteint
    Equity1_22[j]=(Stop1_22[j]-NiveauEntree1_22[j]);            //   On calcule la - value
    ObjHausse1_22[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_22[j] <>0) ) then begin 
    Equity1_22[j]=(c[j]-NiveauEntree1_22[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 22h35: On sort de position
    ObjHausse1_22[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_22[j] <>0) then begin
  if (l[j]<ObjBaisse1_22[j]) then begin              // Si l'objectif a été atteint
    Equity1_22[j]=(NiveauEntree1_22[j] - ObjBaisse1_22[j]);   //   on calcule la + value
    ObjBaisse1_22[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_22[j]) then begin                   // Si le stop a été atteint
    Equity1_22[j]=(NiveauEntree1_22[j] - Stop1_22[j]);        //   On calcule la - value
    ObjBaisse1_22[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_22[j] <>0) ) then begin 
    Equity1_22[j]=(NiveauEntree1_22[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 22h35: On sort de position
    ObjBaisse1_22[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity22[j]=Equity1_22[j];
  Equity1_22[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot22[j] = EquityTot22[j] + Equity22[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 10
end     //----------------------------------------------------------------------------decembre2228
end     //----------------------------------------------------------------------------decembre2228
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2218
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2218



// ici commence le calcul des PV pour 12

ObjRange10e23[j]=12;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille23[j] = ObjRangeTaille23[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour23[j]=NumsignalJour23[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_23[j]=NiveauEntree1_23[j+1];
ObjHausse1_23[j]=ObjHausse1_23[j+1];
ObjBaisse1_23[j]=ObjBaisse1_23[j+1];
Stop1_23[j]=Stop1_23[j+1];
Equity1_23[j]=Equity1_23[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour23[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille23[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille23[j]= (ObjRange10e23[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose23[j]=100;
 if c[j]<BasRange[j] then Pose23[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal23[j]=0;
if ((Pose23[j+1]<=0.23) and (Pose23[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille23[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal23[j]=50;
    NumsignalJour23[j]=NumsignalJour23[j]+1;                   // et on donne un numéro de signal
    ObjHausse23[j]= (HautRange[j] + ObjRangeTaille23[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose23[j+1]>=0) and (Pose23[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille23[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal23[j]=-50;
    NumsignalJour23[j]=NumsignalJour23[j]+1;                   // et on donne un numéro de signal
    ObjBaisse23[j]= (BasRange[j] - ObjRangeTaille23[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour23[j]=1) and (NumsignalJour23[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal23[j]=50) then begin // si signal d'achat
    NiveauEntree1_23[j]=c[j]; //
    ObjHausse1_23[j]=ObjHausse23[j];
    Stop1_23[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_23[j]=c[j]; //
      ObjBaisse1_23[j]=ObjBaisse23[j];
      Stop1_23[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_23[j] <>0) then begin
  if (h[j]>ObjHausse1_23[j]) then begin                // Si l'objectif a été atteint
    Equity1_23[j]=(ObjHausse1_23[j]-NiveauEntree1_23[j]);       //   on calcule la + value
    ObjHausse1_23[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_23[j]) then begin                     // Si le stop a été atteint
    Equity1_23[j]=(Stop1_23[j]-NiveauEntree1_23[j]);            //   On calcule la - value
    ObjHausse1_23[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_23[j] <>0) ) then begin 
    Equity1_23[j]=(c[j]-NiveauEntree1_23[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 23h35: On sort de position
    ObjHausse1_23[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_23[j] <>0) then begin
  if (l[j]<ObjBaisse1_23[j]) then begin              // Si l'objectif a été atteint
    Equity1_23[j]=(NiveauEntree1_23[j] - ObjBaisse1_23[j]);   //   on calcule la + value
    ObjBaisse1_23[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_23[j]) then begin                   // Si le stop a été atteint
    Equity1_23[j]=(NiveauEntree1_23[j] - Stop1_23[j]);        //   On calcule la - value
    ObjBaisse1_23[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_23[j] <>0) ) then begin 
    Equity1_23[j]=(NiveauEntree1_23[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 23h35: On sort de position
    ObjBaisse1_23[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity23[j]=Equity1_23[j];
  Equity1_23[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot23[j] = EquityTot23[j] + Equity23[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 12
end     //----------------------------------------------------------------------------decembre2218
end     //----------------------------------------------------------------------------decembre22218
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2218
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2218



// ici commence le calcul des PV pour 14

ObjRange10e24[j]=14;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille24[j] = ObjRangeTaille24[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour24[j]=NumsignalJour24[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_24[j]=NiveauEntree1_24[j+1];
ObjHausse1_24[j]=ObjHausse1_24[j+1];
ObjBaisse1_24[j]=ObjBaisse1_24[j+1];
Stop1_24[j]=Stop1_24[j+1];
Equity1_24[j]=Equity1_24[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour24[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille24[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille24[j]= (ObjRange10e24[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose24[j]=100;
 if c[j]<BasRange[j] then Pose24[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal24[j]=0;
if ((Pose24[j+1]<=0.24) and (Pose24[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille24[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal24[j]=50;
    NumsignalJour24[j]=NumsignalJour24[j]+1;                   // et on donne un numéro de signal
    ObjHausse24[j]= (HautRange[j] + ObjRangeTaille24[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose24[j+1]>=0) and (Pose24[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille24[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal24[j]=-50;
    NumsignalJour24[j]=NumsignalJour24[j]+1;                   // et on donne un numéro de signal
    ObjBaisse24[j]= (BasRange[j] - ObjRangeTaille24[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour24[j]=1) and (NumsignalJour24[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal24[j]=50) then begin // si signal d'achat
    NiveauEntree1_24[j]=c[j]; //
    ObjHausse1_24[j]=ObjHausse24[j];
    Stop1_24[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_24[j]=c[j]; //
      ObjBaisse1_24[j]=ObjBaisse24[j];
      Stop1_24[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_24[j] <>0) then begin
  if (h[j]>ObjHausse1_24[j]) then begin                // Si l'objectif a été atteint
    Equity1_24[j]=(ObjHausse1_24[j]-NiveauEntree1_24[j]);       //   on calcule la + value
    ObjHausse1_24[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_24[j]) then begin                     // Si le stop a été atteint
    Equity1_24[j]=(Stop1_24[j]-NiveauEntree1_24[j]);            //   On calcule la - value
    ObjHausse1_24[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_24[j] <>0) ) then begin 
    Equity1_24[j]=(c[j]-NiveauEntree1_24[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 24h35: On sort de position
    ObjHausse1_24[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_24[j] <>0) then begin
  if (l[j]<ObjBaisse1_24[j]) then begin              // Si l'objectif a été atteint
    Equity1_24[j]=(NiveauEntree1_24[j] - ObjBaisse1_24[j]);   //   on calcule la + value
    ObjBaisse1_24[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_24[j]) then begin                   // Si le stop a été atteint
    Equity1_24[j]=(NiveauEntree1_24[j] - Stop1_24[j]);        //   On calcule la - value
    ObjBaisse1_24[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_24[j] <>0) ) then begin 
    Equity1_24[j]=(NiveauEntree1_24[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 24h35: On sort de position
    ObjBaisse1_24[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity24[j]=Equity1_24[j];
  Equity1_24[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot24[j] = EquityTot24[j] + Equity24[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 14
end     //----------------------------------------------------------------------------decembre2218
end     //----------------------------------------------------------------------------decembre22218
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2218
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2218



// ici commence le calcul des PV pour 16

ObjRange10e25[j]=16;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille25[j] = ObjRangeTaille25[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour25[j]=NumsignalJour25[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_25[j]=NiveauEntree1_25[j+1];
ObjHausse1_25[j]=ObjHausse1_25[j+1];
ObjBaisse1_25[j]=ObjBaisse1_25[j+1];
Stop1_25[j]=Stop1_25[j+1];
Equity1_25[j]=Equity1_25[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour25[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille25[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille25[j]= (ObjRange10e25[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose25[j]=100;
 if c[j]<BasRange[j] then Pose25[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal25[j]=0;
if ((Pose25[j+1]<=0.25) and (Pose25[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille25[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal25[j]=50;
    NumsignalJour25[j]=NumsignalJour25[j]+1;                   // et on donne un numéro de signal
    ObjHausse25[j]= (HautRange[j] + ObjRangeTaille25[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose25[j+1]>=0) and (Pose25[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille25[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal25[j]=-50;
    NumsignalJour25[j]=NumsignalJour25[j]+1;                   // et on donne un numéro de signal
    ObjBaisse25[j]= (BasRange[j] - ObjRangeTaille25[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour25[j]=1) and (NumsignalJour25[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal25[j]=50) then begin // si signal d'achat
    NiveauEntree1_25[j]=c[j]; //
    ObjHausse1_25[j]=ObjHausse25[j];
    Stop1_25[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_25[j]=c[j]; //
      ObjBaisse1_25[j]=ObjBaisse25[j];
      Stop1_25[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_25[j] <>0) then begin
  if (h[j]>ObjHausse1_25[j]) then begin                // Si l'objectif a été atteint
    Equity1_25[j]=(ObjHausse1_25[j]-NiveauEntree1_25[j]);       //   on calcule la + value
    ObjHausse1_25[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_25[j]) then begin                     // Si le stop a été atteint
    Equity1_25[j]=(Stop1_25[j]-NiveauEntree1_25[j]);            //   On calcule la - value
    ObjHausse1_25[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_25[j] <>0) ) then begin 
    Equity1_25[j]=(c[j]-NiveauEntree1_25[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 25h35: On sort de position
    ObjHausse1_25[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_25[j] <>0) then begin
  if (l[j]<ObjBaisse1_25[j]) then begin              // Si l'objectif a été atteint
    Equity1_25[j]=(NiveauEntree1_25[j] - ObjBaisse1_25[j]);   //   on calcule la + value
    ObjBaisse1_25[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_25[j]) then begin                   // Si le stop a été atteint
    Equity1_25[j]=(NiveauEntree1_25[j] - Stop1_25[j]);        //   On calcule la - value
    ObjBaisse1_25[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_25[j] <>0) ) then begin 
    Equity1_25[j]=(NiveauEntree1_25[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 25h35: On sort de position
    ObjBaisse1_25[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity25[j]=Equity1_25[j];
  Equity1_25[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot25[j] = EquityTot25[j] + Equity25[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 16
end     //----------------------------------------------------------------------------decembre2218
end     //----------------------------------------------------------------------------decembre22218
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2218
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2218



// ici commence le calcul des PV pour 18

ObjRange10e26[j]=18;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille26[j] = ObjRangeTaille26[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour26[j]=NumsignalJour26[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_26[j]=NiveauEntree1_26[j+1];
ObjHausse1_26[j]=ObjHausse1_26[j+1];
ObjBaisse1_26[j]=ObjBaisse1_26[j+1];
Stop1_26[j]=Stop1_26[j+1];
Equity1_26[j]=Equity1_26[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour26[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille26[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille26[j]= (ObjRange10e26[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose26[j]=100;
 if c[j]<BasRange[j] then Pose26[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal26[j]=0;
if ((Pose26[j+1]<=0.26) and (Pose26[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille26[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal26[j]=50;
    NumsignalJour26[j]=NumsignalJour26[j]+1;                   // et on donne un numéro de signal
    ObjHausse26[j]= (HautRange[j] + ObjRangeTaille26[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose26[j+1]>=0) and (Pose26[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille26[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal26[j]=-50;
    NumsignalJour26[j]=NumsignalJour26[j]+1;                   // et on donne un numéro de signal
    ObjBaisse26[j]= (BasRange[j] - ObjRangeTaille26[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour26[j]=1) and (NumsignalJour26[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal26[j]=50) then begin // si signal d'achat
    NiveauEntree1_26[j]=c[j]; //
    ObjHausse1_26[j]=ObjHausse26[j];
    Stop1_26[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_26[j]=c[j]; //
      ObjBaisse1_26[j]=ObjBaisse26[j];
      Stop1_26[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_26[j] <>0) then begin
  if (h[j]>ObjHausse1_26[j]) then begin                // Si l'objectif a été atteint
    Equity1_26[j]=(ObjHausse1_26[j]-NiveauEntree1_26[j]);       //   on calcule la + value
    ObjHausse1_26[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_26[j]) then begin                     // Si le stop a été atteint
    Equity1_26[j]=(Stop1_26[j]-NiveauEntree1_26[j]);            //   On calcule la - value
    ObjHausse1_26[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_26[j] <>0) ) then begin 
    Equity1_26[j]=(c[j]-NiveauEntree1_26[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 26h35: On sort de position
    ObjHausse1_26[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_26[j] <>0) then begin
  if (l[j]<ObjBaisse1_26[j]) then begin              // Si l'objectif a été atteint
    Equity1_26[j]=(NiveauEntree1_26[j] - ObjBaisse1_26[j]);   //   on calcule la + value
    ObjBaisse1_26[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_26[j]) then begin                   // Si le stop a été atteint
    Equity1_26[j]=(NiveauEntree1_26[j] - Stop1_26[j]);        //   On calcule la - value
    ObjBaisse1_26[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_26[j] <>0) ) then begin 
    Equity1_26[j]=(NiveauEntree1_26[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 26h35: On sort de position
    ObjBaisse1_26[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity26[j]=Equity1_26[j];
  Equity1_26[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot26[j] = EquityTot26[j] + Equity26[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 18
end     //----------------------------------------------------------------------------decembre2218
end     //----------------------------------------------------------------------------decembre2218
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre2218
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre2218


ObjRange10e27[j]=20;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille27[j] = ObjRangeTaille27[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour27[j]=NumsignalJour27[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_27[j]=NiveauEntree1_27[j+1];
ObjHausse1_27[j]=ObjHausse1_27[j+1];
ObjBaisse1_27[j]=ObjBaisse1_27[j+1];
Stop1_27[j]=Stop1_27[j+1];
Equity1_27[j]=Equity1_27[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour27[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille27[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille27[j]= (ObjRange10e27[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose27[j]=100;
 if c[j]<BasRange[j] then Pose27[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal27[j]=0;
if ((Pose27[j+1]<=0.27) and (Pose27[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille27[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal27[j]=50;
    NumsignalJour27[j]=NumsignalJour27[j]+1;                   // et on donne un numéro de signal
    ObjHausse27[j]= (HautRange[j] + ObjRangeTaille27[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose27[j+1]>=0) and (Pose27[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille27[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal27[j]=-50;
    NumsignalJour27[j]=NumsignalJour27[j]+1;                   // et on donne un numéro de signal
    ObjBaisse27[j]= (BasRange[j] - ObjRangeTaille27[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour27[j]=1) and (NumsignalJour27[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal27[j]=50) then begin // si signal d'achat
    NiveauEntree1_27[j]=c[j]; //
    ObjHausse1_27[j]=ObjHausse27[j];
    Stop1_27[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_27[j]=c[j]; //
      ObjBaisse1_27[j]=ObjBaisse27[j];
      Stop1_27[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_27[j] <>0) then begin
  if (h[j]>ObjHausse1_27[j]) then begin                // Si l'objectif a été atteint
    Equity1_27[j]=(ObjHausse1_27[j]-NiveauEntree1_27[j]);       //   on calcule la + value
    ObjHausse1_27[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_27[j]) then begin                     // Si le stop a été atteint
    Equity1_27[j]=(Stop1_27[j]-NiveauEntree1_27[j]);            //   On calcule la - value
    ObjHausse1_27[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_27[j] <>0) ) then begin 
    Equity1_27[j]=(c[j]-NiveauEntree1_27[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 27h35: On sort de position
    ObjHausse1_27[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_27[j] <>0) then begin
  if (l[j]<ObjBaisse1_27[j]) then begin              // Si l'objectif a été atteint
    Equity1_27[j]=(NiveauEntree1_27[j] - ObjBaisse1_27[j]);   //   on calcule la + value
    ObjBaisse1_27[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_27[j]) then begin                   // Si le stop a été atteint
    Equity1_27[j]=(NiveauEntree1_27[j] - Stop1_27[j]);        //   On calcule la - value
    ObjBaisse1_27[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_27[j] <>0) ) then begin 
    Equity1_27[j]=(NiveauEntree1_27[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 27h35: On sort de position
    ObjBaisse1_27[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity27[j]=Equity1_27[j];
  Equity1_27[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot27[j] = EquityTot27[j] + Equity27[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 20
end     //----------------------------------------------------------------------------decembre2218
end     //----------------------------------------------------------------------------decembre2218
// ===================================================================================================

// ===================================================================================================


// ===================================================================================================

  end  // C'est le end de la condition qui shunte le calcul si l'heure demandée pour le range est zéro

// ************************************************************************************************************************************************* Fin Range 3



// **************************************************************************************************************************************************** Range 4

if ($EndHour4 =0) then begin
  EquityTot31=-1000;
  EquityTot32=-1000;
  EquityTot33=-1000;
  EquityTot34=-1000;    // Ici on empèche le calcul pour $EndHour2 si l'heure demandée est zéro
  EquityTot35=-1000;
  EquityTot36=-1000;
  EquityTot37=-1000;
  end
  else begin


// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre0328
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre0328

// Partie commune ====================================================================================
    FinTemps[j] = $EndHour4;
    for i=0 to ($BarsInADay) begin // calcul des plus haut et plus bas du range pour chaque jour considéré
      if (TimeToNumeric(timeopen[j]) = FinTemps[j]) then begin // Quand c'est la fin de période du range
        HautRange[j]=haut[j];
        BasRange[j]=bas[j];
      End
      else begin
        HautRange[j]=HautRange[j+1];
        BasRange[j]=BasRange[j+1];
      end
    end

// ici commence le calcul des PV pour 8

ObjRange10e31[j]=8;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille31[j] = ObjRangeTaille31[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour31[j]=NumsignalJour31[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_31[j]=NiveauEntree1_31[j+1];
ObjHausse1_31[j]=ObjHausse1_31[j+1];
ObjBaisse1_31[j]=ObjBaisse1_31[j+1];
Stop1_31[j]=Stop1_31[j+1];
Equity1_31[j]=Equity1_31[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour31[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille31[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille31[j]= (ObjRange10e31[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose31[j]=100;
 if c[j]<BasRange[j] then Pose31[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal31[j]=0;
if ((Pose31[j+1]<=0.31) and (Pose31[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille31[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal31[j]=50;
    NumsignalJour31[j]=NumsignalJour31[j]+1;                   // et on donne un numéro de signal
    ObjHausse31[j]= (HautRange[j] + ObjRangeTaille31[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose31[j+1]>=0) and (Pose31[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille31[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal31[j]=-50;
    NumsignalJour31[j]=NumsignalJour31[j]+1;                   // et on donne un numéro de signal
    ObjBaisse31[j]= (BasRange[j] - ObjRangeTaille31[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour31[j]=1) and (NumsignalJour31[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal31[j]=50) then begin // si signal d'achat
    NiveauEntree1_31[j]=c[j]; //
    ObjHausse1_31[j]=ObjHausse31[j];
    Stop1_31[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_31[j]=c[j]; //
      ObjBaisse1_31[j]=ObjBaisse31[j];
      Stop1_31[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_31[j] <>0) then begin
  if (h[j]>ObjHausse1_31[j]) then begin                // Si l'objectif a été atteint
    Equity1_31[j]=(ObjHausse1_31[j]-NiveauEntree1_31[j]);       //   on calcule la + value
    ObjHausse1_31[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_31[j]) then begin                     // Si le stop a été atteint
    Equity1_31[j]=(Stop1_31[j]-NiveauEntree1_31[j]);            //   On calcule la - value
    ObjHausse1_31[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_31[j] <>0) ) then begin 
    Equity1_31[j]=(c[j]-NiveauEntree1_31[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 31h35: On sort de position
    ObjHausse1_31[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_31[j] <>0) then begin
  if (l[j]<ObjBaisse1_31[j]) then begin              // Si l'objectif a été atteint
    Equity1_31[j]=(NiveauEntree1_31[j] - ObjBaisse1_31[j]);   //   on calcule la + value
    ObjBaisse1_31[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_31[j]) then begin                   // Si le stop a été atteint
    Equity1_31[j]=(NiveauEntree1_31[j] - Stop1_31[j]);        //   On calcule la - value
    ObjBaisse1_31[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_31[j] <>0) ) then begin 
    Equity1_31[j]=(NiveauEntree1_31[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 31h35: On sort de position
    ObjBaisse1_31[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity31[j]=Equity1_31[j];
  Equity1_31[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot31[j] = EquityTot31[j] + Equity31[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 8
end     //----------------------------------------------------------------------------decembre0328
end     //----------------------------------------------------------------------------decembre0328
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3228
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3228



// ici commence le calcul des PV pour 10

ObjRange10e32[j]=10;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille32[j] = ObjRangeTaille32[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour32[j]=NumsignalJour32[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_32[j]=NiveauEntree1_32[j+1];
ObjHausse1_32[j]=ObjHausse1_32[j+1];
ObjBaisse1_32[j]=ObjBaisse1_32[j+1];
Stop1_32[j]=Stop1_32[j+1];
Equity1_32[j]=Equity1_32[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour32[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille32[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille32[j]= (ObjRange10e32[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose32[j]=100;
 if c[j]<BasRange[j] then Pose32[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal32[j]=0;
if ((Pose32[j+1]<=0.32) and (Pose32[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille32[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal32[j]=50;
    NumsignalJour32[j]=NumsignalJour32[j]+1;                   // et on donne un numéro de signal
    ObjHausse32[j]= (HautRange[j] + ObjRangeTaille32[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose32[j+1]>=0) and (Pose32[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille32[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal32[j]=-50;
    NumsignalJour32[j]=NumsignalJour32[j]+1;                   // et on donne un numéro de signal
    ObjBaisse32[j]= (BasRange[j] - ObjRangeTaille32[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour32[j]=1) and (NumsignalJour32[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal32[j]=50) then begin // si signal d'achat
    NiveauEntree1_32[j]=c[j]; //
    ObjHausse1_32[j]=ObjHausse32[j];
    Stop1_32[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_32[j]=c[j]; //
      ObjBaisse1_32[j]=ObjBaisse32[j];
      Stop1_32[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_32[j] <>0) then begin
  if (h[j]>ObjHausse1_32[j]) then begin                // Si l'objectif a été atteint
    Equity1_32[j]=(ObjHausse1_32[j]-NiveauEntree1_32[j]);       //   on calcule la + value
    ObjHausse1_32[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_32[j]) then begin                     // Si le stop a été atteint
    Equity1_32[j]=(Stop1_32[j]-NiveauEntree1_32[j]);            //   On calcule la - value
    ObjHausse1_32[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_32[j] <>0) ) then begin 
    Equity1_32[j]=(c[j]-NiveauEntree1_32[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 32h35: On sort de position
    ObjHausse1_32[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_32[j] <>0) then begin
  if (l[j]<ObjBaisse1_32[j]) then begin              // Si l'objectif a été atteint
    Equity1_32[j]=(NiveauEntree1_32[j] - ObjBaisse1_32[j]);   //   on calcule la + value
    ObjBaisse1_32[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_32[j]) then begin                   // Si le stop a été atteint
    Equity1_32[j]=(NiveauEntree1_32[j] - Stop1_32[j]);        //   On calcule la - value
    ObjBaisse1_32[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_32[j] <>0) ) then begin 
    Equity1_32[j]=(NiveauEntree1_32[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 32h35: On sort de position
    ObjBaisse1_32[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity32[j]=Equity1_32[j];
  Equity1_32[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot32[j] = EquityTot32[j] + Equity32[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 10
end     //----------------------------------------------------------------------------decembre3228
end     //----------------------------------------------------------------------------decembre3228
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3318
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3318



// ici commence le calcul des PV pour 12

ObjRange10e33[j]=12;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille33[j] = ObjRangeTaille33[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour33[j]=NumsignalJour33[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_33[j]=NiveauEntree1_33[j+1];
ObjHausse1_33[j]=ObjHausse1_33[j+1];
ObjBaisse1_33[j]=ObjBaisse1_33[j+1];
Stop1_33[j]=Stop1_33[j+1];
Equity1_33[j]=Equity1_33[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour33[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille33[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille33[j]= (ObjRange10e33[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose33[j]=100;
 if c[j]<BasRange[j] then Pose33[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal33[j]=0;
if ((Pose33[j+1]<=0.33) and (Pose33[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille33[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal33[j]=50;
    NumsignalJour33[j]=NumsignalJour33[j]+1;                   // et on donne un numéro de signal
    ObjHausse33[j]= (HautRange[j] + ObjRangeTaille33[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose33[j+1]>=0) and (Pose33[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille33[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal33[j]=-50;
    NumsignalJour33[j]=NumsignalJour33[j]+1;                   // et on donne un numéro de signal
    ObjBaisse33[j]= (BasRange[j] - ObjRangeTaille33[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour33[j]=1) and (NumsignalJour33[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal33[j]=50) then begin // si signal d'achat
    NiveauEntree1_33[j]=c[j]; //
    ObjHausse1_33[j]=ObjHausse33[j];
    Stop1_33[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_33[j]=c[j]; //
      ObjBaisse1_33[j]=ObjBaisse33[j];
      Stop1_33[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_33[j] <>0) then begin
  if (h[j]>ObjHausse1_33[j]) then begin                // Si l'objectif a été atteint
    Equity1_33[j]=(ObjHausse1_33[j]-NiveauEntree1_33[j]);       //   on calcule la + value
    ObjHausse1_33[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_33[j]) then begin                     // Si le stop a été atteint
    Equity1_33[j]=(Stop1_33[j]-NiveauEntree1_33[j]);            //   On calcule la - value
    ObjHausse1_33[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_33[j] <>0) ) then begin 
    Equity1_33[j]=(c[j]-NiveauEntree1_33[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 33h35: On sort de position
    ObjHausse1_33[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_33[j] <>0) then begin
  if (l[j]<ObjBaisse1_33[j]) then begin              // Si l'objectif a été atteint
    Equity1_33[j]=(NiveauEntree1_33[j] - ObjBaisse1_33[j]);   //   on calcule la + value
    ObjBaisse1_33[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_33[j]) then begin                   // Si le stop a été atteint
    Equity1_33[j]=(NiveauEntree1_33[j] - Stop1_33[j]);        //   On calcule la - value
    ObjBaisse1_33[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_33[j] <>0) ) then begin 
    Equity1_33[j]=(NiveauEntree1_33[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 33h35: On sort de position
    ObjBaisse1_33[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity33[j]=Equity1_33[j];
  Equity1_33[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot33[j] = EquityTot33[j] + Equity33[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 12
end     //----------------------------------------------------------------------------decembre3318
end     //----------------------------------------------------------------------------decembre33318
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3318
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3318



// ici commence le calcul des PV pour 14

ObjRange10e34[j]=14;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille34[j] = ObjRangeTaille34[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour34[j]=NumsignalJour34[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_34[j]=NiveauEntree1_34[j+1];
ObjHausse1_34[j]=ObjHausse1_34[j+1];
ObjBaisse1_34[j]=ObjBaisse1_34[j+1];
Stop1_34[j]=Stop1_34[j+1];
Equity1_34[j]=Equity1_34[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour34[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille34[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille34[j]= (ObjRange10e34[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose34[j]=100;
 if c[j]<BasRange[j] then Pose34[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal34[j]=0;
if ((Pose34[j+1]<=0.34) and (Pose34[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille34[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal34[j]=50;
    NumsignalJour34[j]=NumsignalJour34[j]+1;                   // et on donne un numéro de signal
    ObjHausse34[j]= (HautRange[j] + ObjRangeTaille34[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose34[j+1]>=0) and (Pose34[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille34[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal34[j]=-50;
    NumsignalJour34[j]=NumsignalJour34[j]+1;                   // et on donne un numéro de signal
    ObjBaisse34[j]= (BasRange[j] - ObjRangeTaille34[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour34[j]=1) and (NumsignalJour34[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal34[j]=50) then begin // si signal d'achat
    NiveauEntree1_34[j]=c[j]; //
    ObjHausse1_34[j]=ObjHausse34[j];
    Stop1_34[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_34[j]=c[j]; //
      ObjBaisse1_34[j]=ObjBaisse34[j];
      Stop1_34[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_34[j] <>0) then begin
  if (h[j]>ObjHausse1_34[j]) then begin                // Si l'objectif a été atteint
    Equity1_34[j]=(ObjHausse1_34[j]-NiveauEntree1_34[j]);       //   on calcule la + value
    ObjHausse1_34[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_34[j]) then begin                     // Si le stop a été atteint
    Equity1_34[j]=(Stop1_34[j]-NiveauEntree1_34[j]);            //   On calcule la - value
    ObjHausse1_34[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_34[j] <>0) ) then begin 
    Equity1_34[j]=(c[j]-NiveauEntree1_34[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 34h35: On sort de position
    ObjHausse1_34[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_34[j] <>0) then begin
  if (l[j]<ObjBaisse1_34[j]) then begin              // Si l'objectif a été atteint
    Equity1_34[j]=(NiveauEntree1_34[j] - ObjBaisse1_34[j]);   //   on calcule la + value
    ObjBaisse1_34[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_34[j]) then begin                   // Si le stop a été atteint
    Equity1_34[j]=(NiveauEntree1_34[j] - Stop1_34[j]);        //   On calcule la - value
    ObjBaisse1_34[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_34[j] <>0) ) then begin 
    Equity1_34[j]=(NiveauEntree1_34[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 34h35: On sort de position
    ObjBaisse1_34[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity34[j]=Equity1_34[j];
  Equity1_34[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot34[j] = EquityTot34[j] + Equity34[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 14
end     //----------------------------------------------------------------------------decembre3318
end     //----------------------------------------------------------------------------decembre33318
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3318
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3318



// ici commence le calcul des PV pour 16

ObjRange10e35[j]=16;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille35[j] = ObjRangeTaille35[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour35[j]=NumsignalJour35[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_35[j]=NiveauEntree1_35[j+1];
ObjHausse1_35[j]=ObjHausse1_35[j+1];
ObjBaisse1_35[j]=ObjBaisse1_35[j+1];
Stop1_35[j]=Stop1_35[j+1];
Equity1_35[j]=Equity1_35[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour35[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille35[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille35[j]= (ObjRange10e35[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose35[j]=100;
 if c[j]<BasRange[j] then Pose35[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal35[j]=0;
if ((Pose35[j+1]<=0.35) and (Pose35[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille35[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal35[j]=50;
    NumsignalJour35[j]=NumsignalJour35[j]+1;                   // et on donne un numéro de signal
    ObjHausse35[j]= (HautRange[j] + ObjRangeTaille35[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose35[j+1]>=0) and (Pose35[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille35[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal35[j]=-50;
    NumsignalJour35[j]=NumsignalJour35[j]+1;                   // et on donne un numéro de signal
    ObjBaisse35[j]= (BasRange[j] - ObjRangeTaille35[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour35[j]=1) and (NumsignalJour35[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal35[j]=50) then begin // si signal d'achat
    NiveauEntree1_35[j]=c[j]; //
    ObjHausse1_35[j]=ObjHausse35[j];
    Stop1_35[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_35[j]=c[j]; //
      ObjBaisse1_35[j]=ObjBaisse35[j];
      Stop1_35[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_35[j] <>0) then begin
  if (h[j]>ObjHausse1_35[j]) then begin                // Si l'objectif a été atteint
    Equity1_35[j]=(ObjHausse1_35[j]-NiveauEntree1_35[j]);       //   on calcule la + value
    ObjHausse1_35[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_35[j]) then begin                     // Si le stop a été atteint
    Equity1_35[j]=(Stop1_35[j]-NiveauEntree1_35[j]);            //   On calcule la - value
    ObjHausse1_35[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_35[j] <>0) ) then begin 
    Equity1_35[j]=(c[j]-NiveauEntree1_35[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 35h35: On sort de position
    ObjHausse1_35[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_35[j] <>0) then begin
  if (l[j]<ObjBaisse1_35[j]) then begin              // Si l'objectif a été atteint
    Equity1_35[j]=(NiveauEntree1_35[j] - ObjBaisse1_35[j]);   //   on calcule la + value
    ObjBaisse1_35[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_35[j]) then begin                   // Si le stop a été atteint
    Equity1_35[j]=(NiveauEntree1_35[j] - Stop1_35[j]);        //   On calcule la - value
    ObjBaisse1_35[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_35[j] <>0) ) then begin 
    Equity1_35[j]=(NiveauEntree1_35[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 35h35: On sort de position
    ObjBaisse1_35[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity35[j]=Equity1_35[j];
  Equity1_35[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot35[j] = EquityTot35[j] + Equity35[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 16
end     //----------------------------------------------------------------------------decembre3318
end     //----------------------------------------------------------------------------decembre33318
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3318
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3318



// ici commence le calcul des PV pour 18

ObjRange10e36[j]=18;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille36[j] = ObjRangeTaille36[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour36[j]=NumsignalJour36[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_36[j]=NiveauEntree1_36[j+1];
ObjHausse1_36[j]=ObjHausse1_36[j+1];
ObjBaisse1_36[j]=ObjBaisse1_36[j+1];
Stop1_36[j]=Stop1_36[j+1];
Equity1_36[j]=Equity1_36[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour36[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille36[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille36[j]= (ObjRange10e36[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose36[j]=100;
 if c[j]<BasRange[j] then Pose36[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal36[j]=0;
if ((Pose36[j+1]<=0.36) and (Pose36[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille36[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal36[j]=50;
    NumsignalJour36[j]=NumsignalJour36[j]+1;                   // et on donne un numéro de signal
    ObjHausse36[j]= (HautRange[j] + ObjRangeTaille36[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose36[j+1]>=0) and (Pose36[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille36[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal36[j]=-50;
    NumsignalJour36[j]=NumsignalJour36[j]+1;                   // et on donne un numéro de signal
    ObjBaisse36[j]= (BasRange[j] - ObjRangeTaille36[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour36[j]=1) and (NumsignalJour36[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal36[j]=50) then begin // si signal d'achat
    NiveauEntree1_36[j]=c[j]; //
    ObjHausse1_36[j]=ObjHausse36[j];
    Stop1_36[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_36[j]=c[j]; //
      ObjBaisse1_36[j]=ObjBaisse36[j];
      Stop1_36[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_36[j] <>0) then begin
  if (h[j]>ObjHausse1_36[j]) then begin                // Si l'objectif a été atteint
    Equity1_36[j]=(ObjHausse1_36[j]-NiveauEntree1_36[j]);       //   on calcule la + value
    ObjHausse1_36[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_36[j]) then begin                     // Si le stop a été atteint
    Equity1_36[j]=(Stop1_36[j]-NiveauEntree1_36[j]);            //   On calcule la - value
    ObjHausse1_36[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_36[j] <>0) ) then begin 
    Equity1_36[j]=(c[j]-NiveauEntree1_36[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 36h35: On sort de position
    ObjHausse1_36[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_36[j] <>0) then begin
  if (l[j]<ObjBaisse1_36[j]) then begin              // Si l'objectif a été atteint
    Equity1_36[j]=(NiveauEntree1_36[j] - ObjBaisse1_36[j]);   //   on calcule la + value
    ObjBaisse1_36[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_36[j]) then begin                   // Si le stop a été atteint
    Equity1_36[j]=(NiveauEntree1_36[j] - Stop1_36[j]);        //   On calcule la - value
    ObjBaisse1_36[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_36[j] <>0) ) then begin 
    Equity1_36[j]=(NiveauEntree1_36[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 36h35: On sort de position
    ObjBaisse1_36[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity36[j]=Equity1_36[j];
  Equity1_36[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot36[j] = EquityTot36[j] + Equity36[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 18
end     //----------------------------------------------------------------------------decembre3318
end     //----------------------------------------------------------------------------decembre3318
// ===================================================================================================

// ===================================================================================================

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3318
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3318


ObjRange10e37[j]=20;

RangeDefini[j]=RangeDefini[j+1]; // Le marqueur "RangeDefini" est reconduit à sa valeur précédente
ObjRangeTaille37[j] = ObjRangeTaille37[j+1]; //On reconduit la valeur de l'objectif pour ce jour
NumsignalJour37[j]=NumsignalJour37[j+1]; // On garde la valeur du numéro de signal du jour
NiveauEntree1_37[j]=NiveauEntree1_37[j+1];
ObjHausse1_37[j]=ObjHausse1_37[j+1];
ObjBaisse1_37[j]=ObjBaisse1_37[j+1];
Stop1_37[j]=Stop1_37[j+1];
Equity1_37[j]=Equity1_37[j+1];

if isnewday[j]=1 then begin // on détecte le nouveau jour et on met les marqueurs à 0
  Rangedefini[j]=0;
  NumsignalJour37[j]=0;  // on remet à 0 le numéro de signal du jour
  ObjRangeTaille37[j]=0; //on met à 0 l'objectif car le range n'est pas encore défini en ce début de journée
  Marqueur_IsNewDay[j] = 1; // on crée une série permettant de détecter si IsNewDay
  //HighLight("Slot","Blue");
  end
if ((HautRange[j]<>HautRange[j+1]) and (BasRange[j]<>BasRange[j+1]) ) then begin   // Si un nouveau range est détecté
  RangeDefini[j]=1; // Quand un nouveau range est défini on met le marqueur à 1
  ObjRangeTaille37[j]= (ObjRange10e37[j] * (HautRange[j]-BasRange[j]))/10; // et on calcule l'ampleur de l'objectif par rapport au nouveau range
  end

// Si le range est défini on note dans la série "pose" quand le close est au-dessus ou en dessous ou dans le range et en attente de signal
if (RangeDefini[j]=1) then begin
 if c[j]>HautRange[j] then Pose37[j]=100;
 if c[j]<BasRange[j] then Pose37[j]=-100; 
 end

// Quand "Pose" passe au-dessus de 0 ou en dessous de 0 c'est qu'on a un signal d'entrée long ou court
// On passe alors la série "signal" à 50 ou à -50 (cassure haussière de range ou baissière, zéro quand pas de cassure
signal37[j]=0;
if ((Pose37[j+1]<=0.37) and (Pose37[j]>1)) then begin            // Signal cassure à la hausse
  if (c[j]<(HautRange[j] + (ObjRangeTaille37[j] / 2)))then begin//   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal37[j]=50;
    NumsignalJour37[j]=NumsignalJour37[j]+1;                   // et on donne un numéro de signal
    ObjHausse37[j]= (HautRange[j] + ObjRangeTaille37[j]);         // et on calcule le niveau de l'objectif à la hausse
    end
  End
if ((Pose37[j+1]>=0) and (Pose37[j]<-1)) then begin           // Signal cassure à la baisse
  if (c[j]>(BasRange[j]-(ObjRangeTaille37[j] / 2))) then begin  //   et si l'objectif est suffisament lointain                              DISTANCE MINI DE L'OBJ A ADAPTER. ICI = 1/2 range
    signal37[j]=-50;
    NumsignalJour37[j]=NumsignalJour37[j]+1;                   // et on donne un numéro de signal
    ObjBaisse37[j]= (BasRange[j] - ObjRangeTaille37[j]);          // et on calcule le niveau de l'objectif à la baisse
    end
  end

// Quand le numéro de signal passe de 0 à 1 (Traitement de la première position du jour) 
// On dispose de "Signal" (hausse 50 ou baisse -50 )
//            de c que l'on va stocker pour calculer la +/- value
//            de ObjHausse et ObjBaisse. l'un des deux sera l'objectif. Il faudra les garder en mémoire 
//            de De HautRange et de BasRange qui seront les stops. Il faudra les garder en mémoire 
if ((NumsignalJour37[j]=1) and (NumsignalJour37[j+1]=0)) then begin // cas de la première position prise chaque jour
  if (signal37[j]=50) then begin // si signal d'achat
    NiveauEntree1_37[j]=c[j]; //
    ObjHausse1_37[j]=ObjHausse37[j];
    Stop1_37[j]=BasRange[j];
    end  // ------------------fin signal d'achat
    Else begin             // si signal vente
      NiveauEntree1_37[j]=c[j]; //
      ObjBaisse1_37[j]=ObjBaisse37[j];
      Stop1_37[j]=HautRange[j];
      end //----------------- Fin signal de vente
  end

// Si on est en position haussière en premiere position du jour
if (ObjHausse1_37[j] <>0) then begin
  if (h[j]>ObjHausse1_37[j]) then begin                // Si l'objectif a été atteint
    Equity1_37[j]=(ObjHausse1_37[j]-NiveauEntree1_37[j]);       //   on calcule la + value
    ObjHausse1_37[j]=0;                             //   On remet ObjHausse à 0 car il a été atteint
    end
  if (l[j]<Stop1_37[j]) then begin                     // Si le stop a été atteint
    Equity1_37[j]=(Stop1_37[j]-NiveauEntree1_37[j]);            //   On calcule la - value
    ObjHausse1_37[j]=0;                             //   On remet l'objHausse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])>=$HourToExit) and (ObjHausse1_37[j] <>0) ) then begin 
    Equity1_37[j]=(c[j]-NiveauEntree1_37[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 37h35: On sort de position
    ObjHausse1_37[j]=0;                              //   On remet l'objHausse à 0
    end
  End
// Si on est en position baissière en premiere position du jour
if (ObjBaisse1_37[j] <>0) then begin
  if (l[j]<ObjBaisse1_37[j]) then begin              // Si l'objectif a été atteint
    Equity1_37[j]=(NiveauEntree1_37[j] - ObjBaisse1_37[j]);   //   on calcule la + value
    ObjBaisse1_37[j]=0;                           //   On remet l'objBaisse à 0 car il a été atteint
    end
  if (h[j]>Stop1_37[j]) then begin                   // Si le stop a été atteint
    Equity1_37[j]=(NiveauEntree1_37[j] - Stop1_37[j]);        //   On calcule la - value
    ObjBaisse1_37[j]=0;                           //   On remet l'objBaisse à 0 car le stop a été atteint
    end
  if ( (TimeToNumeric(timeopen[j])=$HourToExit) and (ObjBaisse1_37[j] <>0) ) then begin 
    Equity1_37[j]=(NiveauEntree1_37[j] -c[j]);                 // Si on n'a pas atteint le stop ni l'objectif à 37h35: On sort de position
    ObjBaisse1_37[j]=0;                              //   On remet l'objHausse à 0
    end
  End

// Remise à 0 des equityX quand on entame une nouvelle journée on cumule les équity du jour puis on calcule l'Equity totale sur x jours
if isnewday[j]=1 then begin
  Equity37[j]=Equity1_37[j];
  Equity1_37[j]=0;
      // calcul de la PV sur le nombre de jours demandés
  For i=0 to (CBI[j]-(CBI[j]-($BarsInADay*($NbDaysTested-1))) ) begin
    if (Marqueur_isnewday[j+i]=1) then begin
      EquityTot37[j] = EquityTot37[j] + Equity37[j+i];
      end
    end
  end
// Ici finit la calcul de la PV pour 20
end     //----------------------------------------------------------------------------decembre3318
end     //----------------------------------------------------------------------------decembre3318
// ===================================================================================================

// ===================================================================================================


// ===================================================================================================

  end  // C'est le end de la condition qui shunte le calcul si l'heure demandée pour le range est zéro

// ************************************************************************************************************************************************* Fin Range 4


// ===================================================================================================
// ici commence la détection de la meilleure PV antérieure à chaque début de journée


if isfinalbar() then begin//----------------------------------------------------------------------------decembre3728
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3728


if isnewday[j]=1 then begin

  PositionPrise[j]=0;                  // remis à 0 du flag indiquant qu'une position a été prise ce jour ou pas.
  HautRangeDuJour[j]=void;             // Et on efface les valeurs haut et bas du range du jour précédent
  BasRangeDuJour[j]=void;
  Objectif[j]=void;                    // ainsi que les objectifs et stops du jour précédent
  NiveauStop[j]=void;

  MeilleureEquity[j]= EquityTot01[j];
  MeilleureHeureEtObjectif[j]=1;
  MeilleureHeure[j]=$EndHour1;
  MeilleurObjectif[j]=8;  
  if (EquityTot02[j]>EquityTot01[j]) then begin
    MeilleureEquity[j]= EquityTot02[j];
    MeilleureHeureEtObjectif[j]=2;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=10;
    end
  if (EquityTot03[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot03[j];
    MeilleureHeureEtObjectif[j]=3;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=12;
    end 
  if (EquityTot04[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot04[j];
    MeilleureHeureEtObjectif[j]=4;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=14;
    end 
  if (EquityTot05[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot05[j];
    MeilleureHeureEtObjectif[j]=5;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=16;
    end 
  if (EquityTot06[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot06[j];
    MeilleureHeureEtObjectif[j]=6;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=18;
    end 
  if (EquityTot07[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot07[j];
    MeilleureHeureEtObjectif[j]=7;
    MeilleureHeure[j]=$EndHour1;
    MeilleurObjectif[j]=20;
    end 
  if (EquityTot11[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot11[j];
    MeilleureHeureEtObjectif[j]=11;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=8;
    end
  if (EquityTot12[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot12[j];
    MeilleureHeureEtObjectif[j]=12;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=10;
    end 
  if (EquityTot13[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot13[j];
    MeilleureHeureEtObjectif[j]=13;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=12;
    end 
  if (EquityTot14[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot14[j];
    MeilleureHeureEtObjectif[j]=14;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=14;
    end 
  if (EquityTot15[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot15[j];
    MeilleureHeureEtObjectif[j]=15;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=16;
    end 
  if (EquityTot16[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot16[j];
    MeilleureHeureEtObjectif[j]=16;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=18;
    end 
  if (EquityTot17[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot17[j];
    MeilleureHeureEtObjectif[j]=17;
    MeilleureHeure[j]=$EndHour2;
    MeilleurObjectif[j]=20;
    end 
  if (EquityTot21[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot21[j];
    MeilleureHeureEtObjectif[j]=21;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=8;
    end
  if (EquityTot22[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot22[j];
    MeilleureHeureEtObjectif[j]=22;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=10;
    end 
  if (EquityTot23[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot23[j];
    MeilleureHeureEtObjectif[j]=23;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=12;
    end 
  if (EquityTot24[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot24[j];
    MeilleureHeureEtObjectif[j]=24;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=14;
    end 
  if (EquityTot25[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot25[j];
    MeilleureHeureEtObjectif[j]=25;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=16;
    end 
  if (EquityTot26[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot26[j];
    MeilleureHeureEtObjectif[j]=26;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=18;
    end 
  if (EquityTot27[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot27[j];
    MeilleureHeureEtObjectif[j]=27;
    MeilleureHeure[j]=$EndHour3;
    MeilleurObjectif[j]=20;
    end 
  if (EquityTot31[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot31[j];
    MeilleureHeureEtObjectif[j]=31;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=8;
    end
  if (EquityTot32[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot32[j];
    MeilleureHeureEtObjectif[j]=32;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=10;
    end 
  if (EquityTot33[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot33[j];
    MeilleureHeureEtObjectif[j]=33;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=12;
    end 
  if (EquityTot34[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot34[j];
    MeilleureHeureEtObjectif[j]=34;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=14;
    end 
  if (EquityTot35[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot35[j];
    MeilleureHeureEtObjectif[j]=35;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=16;
    end 
  if (EquityTot36[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot36[j];
    MeilleureHeureEtObjectif[j]=36;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=18;
    end 
  if (EquityTot37[j]>MeilleureEquity[j]) then begin
    MeilleureEquity[j]= EquityTot37[j];
    MeilleureHeureEtObjectif[j]=37;
    MeilleureHeure[j]=$EndHour4;
    MeilleurObjectif[j]=20;
    end




  end // end de if isnewday

// ici finit la détection de la meilleure PV. Le meilleur couple range-objectif est stocké à chaque nouveau jour.
// ===================================================================================================
  else begin
// On reconduit les valeurs antérieures (si ce n'est pas une nouvelle journée)
//   de la meilleure heure et du meilleur objectif
//   (Ces valeurs ne seront modifiées qu'en début de journée)
MeilleureHeure[j]=MeilleureHeure[j+1];
MeilleurObjectif[j]=MeilleurObjectif[j+1];

  end



end     //------------------------------end de for j = cbi downto 0 --------------------------decembre3728
end     //------------------------------end de if isfinalbar()--------------------------------decembre3728

if isnewday() then   MessageBox(symbolname()+ " " + "Selected End of Range : " + NumericToString(MeilleureHeure,"%6.0f") +"    Target "+ NumericToString(MeilleurObjectif,"%6.0f") + "  10e Range." );

// ===================================================================================================
// Calcul du sentiment, du stop et de l'objectif

if isfinalbar() then begin//----------------------------------------------------------------------------decembre3728
for j=cbi downto 0 begin//----------------------------------------------------------------------------decembre3728

if isnewday[j]=1 then PositionPrise[j]=0;
else begin
PositionPrise[j]=PositionPrise[j+1];     // De même, on reporte le marqueur de position prise à sa valeur précédente
                                    // Il sera mis à 1 une fois la position prise et remis à 0 à chaque début de journée.
  HautRangeDuJour[j]=HautRangeDuJour[j+1];
  BasRangeDuJour[j]=BasRangeDuJour[j+1];
  ObjRangeTaille[j]=ObjRangeTaille[j+1];
  Objectif[j]=Objectif[j+1];
  NiveauStop[j]=NiveauStop[j+1];
  
end

Senti[j]=50;
// On attend que l'heure soit atteinte pour définir le range de chaque jour
if (TimeToNumeric(timeopen[j]) = MeilleureHeure[j]) then begin // Quand c'est la fin de période du range
  HautRangeDuJour[j]=Haut[j];       // On en stocke le haut et le bas du nouveau range
  BasRangeDuJour[j]= bas[j];//Lowest(LowPrecd,$barsOfRange);
  ObjRangeTaille[j]= (MeilleurObjectif[j]/10) * (HautRangeDuJour[j]-BasRangeDuJour[j]); // On calcule la distance des objectifs par rapport aux bas et hauts de range
  End

// Si les cours cassent le haut du range en clôture on engage une position acheteuse
if (c[j]>HautRangeDuJour[j]) and (c[j+1]<=HautRangeDuJour[j]) and (PositionPrise[j]=0) and ( c[j]<(HautRangeDuJour[j] + (ObjRangeTaille[j] /2)) ) then begin
  PositionPrise[j]=1;
  Senti[j]=100;
  Objectif[j] = HautRangeDuJour[j] + ObjRangeTaille[j];
  NiveauStop[j] = BasRangeDuJour[j];
  end

// Si les cours cassent le bas du range en clôture on engage une position vendeuse
if (c[j]<BasRangeDuJour[j]) and (c[j+1]>=BasRangeDuJour[j]) and (PositionPrise[j]=0) and ( c[j]>(BasRangeDuJour[j] - (ObjRangeTaille[j] /2)) ) then begin
  PositionPrise[j]=-1;
  Senti[j]=0;
  Objectif[j] = BasRangeDuJour[j] - ObjRangeTaille[j];
  NiveauStop[j] = HautRangeDuJour[j];
  end
 
// Sortie à 17h30 (ou autre)
If (TimeToNumeric(timeopen[j]) = $HourToExit) then begin
  if positionPrise[j]>0 then senti[j]=39;
  if PositionPrise[j]<0 then senti[j]=61;
  end

// Sorties de sécurité au cas où les indicateurs de stop et d'objectif n'ont pas été utilisés
if ((PositionPrise[j]=1) and ( (h[j]>=Objectif[j]) or (l[j]<=NiveauStop[j]) )) then senti[j] = 39;
if ((PositionPrise[j]=-1) and ( (l[j]<=Objectif[j]) or (h[j]>=NiveauStop[j]) )) then senti[j]=61;


// effacement du range si l'objectif ou le stop ou l'heure limite ont été atteints
if ((PositionPrise[j+1]>0) and ( (h[j+1]>=Objectif[j]) or (l[j+1]<=NiveauStop[j]) )) or (TimeToNumeric(timeopen[j]) > $HourToExit) then begin
  HautRangeDuJour[j]=void;
  BasRangeDuJour[j]=void;
  Objectif[j]=void;
  NiveauStop[j]=void;
  end
if ((PositionPrise[j+1]=-1) and ( (l[j+1]<=Objectif[j]) or (h[j+1]>=NiveauStop[j]) )) or (TimeToNumeric(timeopen[j]) > $HourToExit) then begin
  HautRangeDuJour[j]=void;
  BasRangeDuJour[j]=void;
  Objectif[j]=void;
  NiveauStop[j]=void;
  end

if (date[j]>01_01_2050) then begin
  senti[j]=50;                     // fin de gratuité au 1/1/2020
  HautRangeDuJour[j]=void;
  BasRangeDuJour[j]=void;
  end

end     //------------------------------end de for j = cbi downto 0 --------------------------decembre2018
end     //------------------------------end de if isfinalbar()--------------------------------decembre2018


Interpretation 
begin
  Sentiment=senti;
end



//plot (HautRange,green,2);
//plot (BasRange,red,2);
//plot (pose03,green,1);
//plot (Signal,green,2);
//plot (NumSignalJour,black,1);
//plot (Equity1_27,blue,2);
//plot (EquityTot22,red,2);
//plot (Equity31,green,2);
//plot (meilleurobjectif,red,2);
//plot (Meilleureheure,black,1);
//plot(fintemps,blue,1);
//plot(timeop,blue,1);
//plot(positionprise,black,1);

//plot (MeilleureEquity,black,2);
//plot (MeilleureHeureEtObjectif,red,2);
//plot (PositionPrise,black,1);
//plot(haut,black,2);

plot (HautRangeDuJour,green,2);
Plot (BasRangeDuJour,red,2);
Plot (Objectif,green,1);
Plot (NiveauStop,red,1);//@@@cs:76936882-43254421-64959074_cs@@@